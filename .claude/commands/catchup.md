Re-read `AGENTS.md`, `.agents/state.md`, and the active repo docs, then summarize
current state, the next useful action, any blockers, and one proposed first action.
Make no changes until Michael responds. See the "Operator Requests" section of
`AGENTS.md` for the canonical definition.
