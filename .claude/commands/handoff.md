Update `.agents/state.md` so the next session can resume without chat context:
current active work, next action, blockers, and verification commands. Do not import
historical narrative. See the "Operator Requests" section of `AGENTS.md` for the
canonical definition.
