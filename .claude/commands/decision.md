Record a settled durable decision in `.agents/decisions.md` and update any affected
guidance. Write it so it makes sense without chat history; name superseded guidance
where relevant. See the "Operator Requests" section of `AGENTS.md` for the canonical
definition.
