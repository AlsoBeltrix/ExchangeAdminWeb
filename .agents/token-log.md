# Token Log

One line per landed slice, appended in the same commit that lands the slice - part of
the existing paperwork motion, not a separate ritual (docs/TokenBudget-Plan.md S3, AC9).

Figures come from `tools/Get-TokenUsage.ps1 -GroupBy Session` for the session that
produced the slice, on this machine's transcripts only. Costs are ESTIMATES at
first-party list rates (the tool prints its rate table); the model column is the model
that implemented the slice. Slices sharing one session share one line, labelled so.

The committed baseline these compare against is `.agents/token-baseline.json`
(August 2026; regenerate on or after 2026-09-01 so the full month is in it).

Format: `date  slice  model  reqs N  mean-ctx NK  est $X.XX`

2026-08-27  TokenBudget S1+S2  fable-5 (est at opus-5 rates)  reqs 62  mean-ctx 76K  est $6.79
2026-08-27  TokenBudget S3+S4  fable-5 (est at opus-5 rates)  reqs 64  mean-ctx 79K  est $7.17  (same session as S1+S2; session total at close, supersedes the line above)
2026-08-27  GroupMemberNesting S1-S6 + gmn-4/5 fixes  fable-5 (est at opus-5 rates)  day-total reqs 179  mean-ctx 225K  est $34.89  (day shared with TokenBudget S1-S4; codex reviews bill on their own harness)
2026-08-28  GroupListingCrossDomainFix  fable-5 (est at opus-5 rates)  reqs 38  mean-ctx 219K  est $8.76  (session figures at commit; session also spans the unauthorised pgwt start and its diagnosis/cleanup)
2026-08-28  lst-1..lst-3 fixes + review loop  fable-5 (est at opus-5 rates)  reqs 70  mean-ctx 312K  est $18.04  (same session; session total at lst-2 close, supersedes the line above; codex reviews bill on their own harness)
2026-08-28  ProtectedGroupWriteTarget S1-S4  fable-5 (est at opus-5 rates)  reqs 90  mean-ctx 359K  est $28.47  (same session; session total at S4 close, supersedes the line above)
2026-08-28  pgwt range-review fixes pgwt-4..9  fable-5 (est at opus-5 rates)  reqs 126  mean-ctx 431K  est $41.59  (same session; session total at loop close, supersedes the line above; codex reviews bill on their own harness)
2026-08-31  pgwt AC4 reversal (self-service target gate removed) + dev browser validation  fable-5 (est at opus-5 rates)  day-total reqs 116  mean-ctx 222K  est $31.17  (day shared with the crashed morning session; codex reviews none)
2026-08-31  GroupSearchForestScope  fable-5 (est at opus-5 rates)  day-total reqs 166  mean-ctx 239K  est $41.28  (same session; supersedes the line above; day shared with the crashed morning session)
2026-08-31  ProtectedTargetQueryGate (GroupManagement 2.6.0)  fable-5 (est at opus-5 rates)  day-total reqs 222  mean-ctx 267K  est $55.04  (same session; supersedes the line above; day shared with the crashed morning session)
2026-08-31  fsr-1 cross-domain routing fix + codereview round  fable-5 (est at opus-5 rates)  day-total reqs 262  mean-ctx 292K  est $67.25  (same session; supersedes the line above; codex generation pass bills on its own harness)
2026-08-31  BitLockerMandatoryTicket plan draft  fable-5 (est at opus-5 rates)  day-total reqs 327  mean-ctx 313K  est $85.03  (new session, same day; supersedes the line above as the day total)
2026-08-31  BitLockerMandatoryTicket plan revision (SNow seam ruling)  fable-5 (est at opus-5 rates)  same session as the plan-draft line above; day total supersedes at session end
2026-09-01  Plans queue: bitlocker ticket plan correction + csv export plan + combined codex openreview (btv-1, csv-1, csv-2 folded)  fable-5 (est at opus-5 rates)  month-to-date reqs 29  mean-ctx 231K  est $6.27  (session began 2026-08-31; that day's totals are on the lines above; codex pass bills on its own harness)
2026-09-01  BitLockerMandatoryTicket S1 (ServiceNowService.Enabled + ITicketValidator/TicketValidationService + DI + 10 tests, 3-batch mutation-proven; base 2.10.0 -> 2.11.0)  fable-5 (est at opus-5 rates)  same session as the 2026-09-01 line above; day total supersedes at session end
2026-09-01  BitLockerMandatoryTicket S2 (service ticket gate on both searches + page ticket input, capture and audit wiring + ValidateTickets Boolean config field; 5 new tests, 2-batch mutation-proven)  fable-5 (est at opus-5 rates)  reqs 26  mean-ctx 109K  est $3.63  (fresh session, figures at commit)
2026-09-01  BitLockerMandatoryTicket S3 (module 1.1.0 + docs; plan Implemented, traceability completed)  fable-5 (est at opus-5 rates)  same session as the S2 line above (owner go mid-session); session total supersedes at close
2026-09-01  BooleanConfigControls S1 (ConfigFieldType.Boolean + checkbox rendering, PreventSelfGrant converted, tripwire; base 2.11.0 -> 2.12.0)  fable-5 (est at opus-5 rates)  same session as the lines above; day total supersedes at session end
2026-09-01  ModuleCsvExport S1 (Services/CsvExport.cs shared writer + AC1b formula neutralization + 5 tests, 3-batch mutation-proven; base 2.12.0 -> 2.13.0)  sonnet-5 subagent (est at opus-5 rates)  reqs 45  mean-ctx 57K  est $3.41  (dispatched by fable-5 in its own session; figures are this subagent's own session)
2026-09-01  ModuleCsvExport S2 (DhcpAuthorization BuildCsv projector + Download CSV button + JS/audit wiring + module 1.2.3 -> 1.3.0; 3 new tests, 2-batch mutation-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 111  mean-ctx 66K  est $7.36  (dispatched by fable-5 in its own session; figures are this subagent's own session)
2026-09-01  ModuleCsvExport S3 (NamedLocations BuildCsv projector + Download CSV button + JS/audit wiring + module 1.0.3 -> 1.1.0; 3 new tests, 3-batch mutation-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 164  mean-ctx 69K  est $10.15  (dispatched by fable-5 in its own session; figures are this subagent's own session)
2026-09-01  ModuleCsvExport S4 (BlockedSenders BuildCsv projector + Download CSV button + JS/audit wiring + module 1.3.0 -> 1.4.0; 3 new tests, 3-batch mutation-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 233  mean-ctx 71K  est $13.99  (dispatched by fable-5 in its own session; figures are this subagent's own session)
2026-09-01  ModuleCsvExport S5 (BitLockerRecovery BuildCsv projector with RecoveryKey + Ticket columns, AC4b ExportRecoveryKeysCsv bulk-disclosure audit, Download CSV button + JS/audit wiring + module 1.1.0 -> 1.2.0; 5 new tests, 3-batch mutation-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 346  mean-ctx 78K  est $20.12  (dispatched by fable-5 in its own session; figures are this subagent's own session)
2026-09-01  ModuleCsvExport S6 (Migration status BuildCsv projector + Download CSV button in status tab + JS/audit wiring + module 1.7.1 -> 1.8.0; 3 new tests, 4-batch mutation-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 447  mean-ctx 81K  est $26.54  (dispatched by fable-5 in its own session; figures are this subagent's own session)
2026-09-01  RiskyUsersModule S1+S3 (descriptor SortOrder 745 + read UI Components/Pages/RiskyUsers.razor over the inherited S2 service + 4 catalog tests + 16 page tests, 12-mutation non-vacuity proven; module 1.0.0)  sonnet-5 subagent (est at opus-5 rates)  reqs 699  mean-ctx 89K  est $45.09  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-01  ModuleCsvExport S7 (README export bullets for DhcpAuthorization, NamedLocations, BitLockerRecovery, Migration, plus a new minimal Blocked Senders section since it had none; docs-only)  sonnet-5 subagent (est at opus-5 rates)  reqs 499  mean-ctx 80K  est $29.44  (dispatched by fable-5 in its own session; figures are this subagent's own session)
2026-09-01  RiskyUsersModule S4+S5 (RiskyUsersServiceTests.cs read-path tests, 6-batch mutation-proven; Services/RiskyUsersService.cs write path ApplyActionAsync one-call-per-user + 6 write-path tests, 4-batch mutation-proven; module stays 1.0.0)  sonnet-5 subagent (est at opus-5 rates)  reqs 812  mean-ctx 92K  est $53.41  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-01  RiskyUsersModule S6 (write UI in Components/Pages/RiskyUsers.razor: per-row Dismiss/Safe/Compromised behind RiskyUsersRemediate, ticket + confirm bar beneath the acting row, two-branch protected-principal check BEFORE the write with EntraObjectId populated from riskyUser.id, servicer override on both branches, audited refusals via a single Refuse path, per-user outcomes, admin notification from finally; + RiskyUsers added to ModuleConfig's servicer opt-in set; 24 new page tests, 4-round 25-mutation non-vacuity proven; module stays 1.0.0)  opus-5 subagent  reqs 883  mean-ctx 96K  est $60.94  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-01  RiskyUsersModule S7 (README section for Risky Users, docs-only, verified module version already 1.0.0)  sonnet-5 subagent (est at opus-5 rates)  reqs 905  mean-ctx 97K  est $63.16  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-02  IntuneSearchAlign (IntuneDevices.razor search card: Search button aligned with the input via order-1/order-2 utility classes so the hint renders full-width below the whole row while its wording stays inside the pinned source window for IntuneDevices_SearchBox_SaysItMatchesTheStartOfANameAndAnExactSerial; no version bump, cosmetic only)  sonnet-5 subagent (est at opus-5 rates)  reqs 2171  mean-ctx 112K  est $180.67  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session, cumulative across slices dispatched into it)
2026-09-01  IntuneDevices S1 finish (Models/IntuneDeviceModels.cs + Services/IntuneDeviceService.cs read-only search/detail + DI registration; found and restored one unrestored mutation probe (SearchDevicesAsync silently returning empty on unavailable Graph client instead of throwing); 28 tests, truncation-flag and GetWithStatusAsync-error-path mutation-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 1090  mean-ctx 97K  est $76.04  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-01  IntuneDevices S2 finish (module descriptor: three fail-closed granular permissions + Boolean-typed D2/D3 default config fields, catalog now 27 modules / 40 aliases; read-only Components/Pages/IntuneDevices.razor + IntuneDevicesPageTests.cs; inherited draft verified clean against the plan, no corrections needed; 2-batch mutation-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 1199  mean-ctx 98K  est $84.18  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-01  IntuneDevices S3+S4 (S3: DeleteDeviceAsync over the S0 status-returning DELETE with 403/404/5xx as distinct outcomes; page delete action behind IntuneDevicesDelete with T6 re-check, ticket, two-branch protected-principal check on the device primary user with EntraObjectId from managedDevice.userId, servicer override visible on screen, audited refusals, admin notification from finally; IntuneDevices added to the ModuleConfig servicer opt-in set; GetGraphClientAsync credential-path tests closed via an ExtractGraphCredentials seam. S4: RetireDeviceAsync body-less and WipeDeviceAsync with an always-explicit body, every Graph wipe parameter as an operator control, wipe requires typing the device name, macOS PIN shown back once and audited as (set)/(not set), audit names the exact flag set. 43 + 33 new tests, 10 mutation probes all proven biting)  opus-5 subagent  reqs 1463  mean-ctx 118K  est $128.24  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-01  IntuneDevices S5+S6 (S5: RemoveEntraDeviceAsync over the S0 status-returning DELETE addressing the alternate key /devices(deviceId='...') - never the path form that 404s on a healthy device - with unusable-id refusal before any request, a fourth IntuneDeviceAction.EntraDelete behind its own IntuneDevicesEntraDelete tier, checkbox beside each Intune action seeded from RemoveEntraObjectByDefault plus a standalone detail-panel action, azureADDeviceId captured before the Intune write, Entra half second and only on Intune success, two outcome fields / two alerts / two audit events / two admin notifications, its own T6 re-check that cannot throw into the Intune audit path, and DeleteSuccessMessageEntraRemoved for AC11. S6: EmailService.SendDeviceActionUserNotificationAsync returning whether it actually sent plus UserNotificationsEnabled, IntuneDeviceService.DecideUserNotification as a pure five-outcome function, per-action checkbox seeded from NotifyUserOn{Delete,Retire,Wipe}, suppression stated at confirm time and in the result and in the audit extra, notification failure never changing the action result. 41 new tests, 7 mutation probes all proven biting; no version bumps, S7 closes)  opus-5 subagent  reqs 1603  mean-ctx 121K  est $143.74  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-01  IntuneDevices S7 (README section for Intune Devices module, docs-only; verified module version already 1.0.0 and base app version already 2.14.0)  sonnet-5 subagent (est at opus-5 rates)  reqs 1480  mean-ctx 109K  est $116.07  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session, taken before the plan/state closing commit)
2026-09-02  SidebarHomeLink (remove redundant sidebar Home NavLink from NavMenu.razor, keep the brand link; dead .nav-home CSS removed from NavMenu.razor.css and app.css; base app version 2.14.0 -> 2.15.0; new NavMenuTests.cs source guard, probe-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 1550  mean-ctx 108K  est $120.46  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session)
2026-09-02  UnsavedChangesCount (AdminPageDirtyState now tracks per-section pending-EDIT counts instead of a set of dirty section names, so the save bar reads "5 unsaved changes in Modules, Protected" where it used to read "1"; Increment for a discrete action with nothing to diff, SetCount(section, n) for a section that diffs its working copy against what it loaded; both admin pages recount from load baselines - module enablement, protected principals, module config, section access, module admins - with Editable Attributes tallying committed edits instead, because its rows have no identity to diff by; baselines re-snapshot on the four saves that do not reload the page; base app version 2.15.0 -> 2.16.0, no module bumps; AdminPageDirtyState tests 14 -> 21, 5 mutation probes all proven biting)  opus-5 subagent  reqs 849  mean-ctx 127K  est $81.46  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent own session, taken before this commit; a concurrent session was editing the Intune module in the same working tree, so this commit was staged by path)
2026-09-02  IntuneNotifyDefaults (owner ruling 2026-09-02: the four Boolean IntuneDevices config fields - NotifyUserOnDelete/Retire/Wipe and RemoveEntraObjectByDefault - removed from the descriptor, leaving GraphDelineaSecretId and SearchResultLimit; the act-time checkboxes stay and now start from fixed states in IntuneDeviceService - NotifyUserStartsTicked off/on/on with null for EntraDelete, EntraRemovalStartsTicked off - with NotifyUserByDefault/NotifyUserConfig/RemoveEntraObjectByDefault/ParseBooleanConfig and their tests deleted; DecideUserNotification and the app-wide suppression visibility untouched; plan D2 Revision 2026-09-02 + README + decisions entry; module 1.1.0, no base bump; 3 tests replaced by 2, 2 probe batches all proven biting)  opus-5 subagent  reqs 882  mean-ctx 127K  est $84.47  (dispatched by an Opus 5 (1M context) orchestrator; this transcript is shared with the concurrent UnsavedChangesCount subagent above, so the figures cover both sessions' work and are taken before this commit)
2026-09-02  PermissionDescriptions (owner ruling 2026-09-02: the Module Config Access tab listed each module's permissions by internal alias alone, so "who knows what you're approving" - ModulePermission gains a REQUIRED Description with no default, positioned third so every construction site had to supply one; all 42 permissions across the 27 modules described in operator terms, including two that honestly describe granting nothing - ExchangeOnline has no consumer of its policy, AdminSettings answers to Security:AdminGroups; Access tab renders each sentence from the descriptor via PermissionDescriptionFor, and no other UI lists permissions - NavMenu/Home read the alias only to authorize; spec + developer guide carry the field and its rule; base app version 2.16.0 -> 2.17.0, no module bumps. Second commit: AddPpPattern and the directory-read secret id edits on AdminSettings now route through RecountProtected, so a pattern-only or secret-id-only edit no longer reads "No unsaved changes" and leaves with no warning; +2 catalog tests, +6 dirty-state tests, 5 mutation probes all proven biting)  opus-5 subagent  reqs 1914  mean-ctx 111K  est $158.09  (dispatched by an Opus 5 (1M context) orchestrator; this transcript is shared with earlier slices from the same session, so the figures cover more than this slice and are taken before this commit)
2026-09-02  RiskyUsersWording (owner ruling 2026-09-02: Dismiss/Safe/Compromised action buttons reworded for L2 support desk staff - "Close as handled" / "This was the real user" / "Account was breached" - each with a title tooltip and a per-row confirmation-bar line stating the consequence verbatim above the ticket controls, via new ActionConsequence(RiskyUserAction) alongside the updated ActionLabel; RiskyUsersService.ActionDisplayName echoes the same labels in the outcome/audit message; Graph endpoints, audit action names and the enum are unchanged; README + plan S6 revision recorded; module 1.0.0 -> 1.1.0, no base bump; 5 new/updated exact-string tests plus 2 source-text guards, non-vacuity probe-proven)  sonnet-5 subagent (est at opus-5 rates)  reqs 1999  mean-ctx 112K  est $166.67  (dispatched by an Opus 5 (1M context) orchestrator in its own session; figures are this subagent's own session, taken before this commit)
2026-09-02  CrossDomainRemove (owner bug, dev 2026-09-02: removing the cross-domain nested group Organization Management (WINROOT) from ExchangeWebAdmins (ANALOG) in Self-Service Groups failed with "The member could not be resolved right now" - the listed rows GUID lookup had no -Server, so it asked the credentials home domain for an object whose partition lives elsewhere; the listing was fixed for this on 2026-08-28, the remove path never was. The row DN now rides with its GUID from page to service so the member resolves in its OWN domain (blank DN falls back to the home domain, still blocking on a miss); the shared membership pre-check/read-back in BOTH modules now asks the GROUP object, in the groups own domain, for its forward member link instead of the members local memberOf back-link, which reported a real cross-domain membership as absent and would have turned the remove into a silent no-op; self-service writes routed by the groups DN like the admin modules; GroupManagement ResolveGroupForWrite name-only fallback now searches the forest global catalog with the same ":3268" guard as the search and re-reads the match in its own domain before gating, so a foreign-domain target is writable and a two-domain namesake is refused rather than swapped. SelfServiceGroups 1.6.0 -> 1.7.0, GroupManagement 2.6.0 -> 2.7.0, no base bump; 6 new tests, 3 probe rounds all proven biting)  opus subagent  reqs 1183  mean-ctx 124K  est $111.08  (dispatched by an Opus 5 (1M context) orchestrator; this transcript is shared with a concurrent Intune-module session, so the figures cover both and are taken before this commit)
2026-09-02  IntuneSearchHelp (two owner findings from dev validation 2026-09-02: (1) the device search was exact-match only and useless as a search box - a full UPN typed on dev returned 200 with an empty value array - so BuildFilterExpression now emits startswith(deviceName,'x') or startswith(userPrincipalName,'x') or serialNumber eq 'x', with contains deliberately unused because managedDevices answers 400 to it, and the search box label, placeholder and a new hint line say which fields match from the START and which must be exact; BuildFailure gained a 400 branch so a filter Graph rejects reads as a named failed search rather than a device that does not exist (T7), Graph's own error text being unquotable because GraphTokenClient's GET helper discards the body on a non-success status; plan T2 Revision 2026-09-02 records eq accepted / startswith now used / contains not used, with startswith still to confirm on the owner's next dev search. (2) plain-English contextual help for the destructive actions, written for L2 support desk staff - new ActionHelp(IntuneDeviceAction), NotifyUserHelp and ActionHelpSummary/FirstSentences render ONE canonical text three ways: a collapsible "What do these actions do?" panel closed by default, a title tooltip per action button (first sentence) and a consequence line above each confirmation bar's ticket box (first two sentences); the panel is toggled from C# because this app ships Bootstrap CSS with no Bootstrap JS, so a data-bs-toggle button would have rendered dead; README carries the same five blocks; module 1.1.0 -> 1.2.0, no base bump; +14 tests, 8 mutation probes all proven biting)  opus-5 subagent  reqs 1205  mean-ctx 124K  est $113.38  (dispatched by an Opus 5 (1M context) orchestrator; this transcript root is shared with two concurrent subagents editing the group modules and Risky Users the same day, so the figures cover more than this slice and are taken before this commit)
2026-09-03  IntuneSearchPerField (owner finding from dev 2026-09-03, module 1.2.0 with the app registration proven: the device search returned 200 with an EMPTY value array for every term - the exact device name a blank search had just listed, a real HYB- prefix, a full UPN - for both the eq and the startswith form, so the defect was the FILTER SHAPE, not the operator: one $filter joining deviceName/userPrincipalName/serialNumber with or, whole-expression Uri.EscapeDataString'd so (, , and ' arrived percent-encoded. The search now issues ONE REQUEST PER FIELD concurrently - startswith(deviceName,'v'), startswith(userPrincipalName,'v'), serialNumber%20eq%20'v', same $top/$select - merged by device id with first occurrence winning, ordered by device name, Truncated if ANY page carried @odata.nextLink, SearchedCount the distinct merged count, blank term still one unfiltered request; encoding narrowed so only the typed value is escaped (single quote doubled first) while OData syntax stays literal, exposed through three internal static URL builders pinned by exact-string tests; T7 is per field - any one non-2xx fails the whole search naming that field's request, never a partial or silent-empty result; and a defensive client-side re-check drops rows that do not actually match, counting them in FilterIgnoredCount which the page states as a warning, so an endpoint that ignores a filter cannot render an arbitrary page as matches. Plan T2 Revision 2026-09-03 + README; module 1.2.0 -> 1.3.0, no base bump; 22 new tests replacing 5, 2 probe rounds (11 + 6 failures) all proven biting)  opus subagent  reqs 49  mean-ctx 119K  est $6.10  (figures are this transcript root for 2026-09-03, which covers this subagent and its dispatching session, taken before this commit)
2026-09-03  CrossDomainWrite (live dev failure 2026-09-03, one commit: after yesterday's routing fix the cross-domain remove got PAST the resolve and failed at the WRITE - CmdletInvocationException "Cannot find an object with identity: 'CN=Organization Management,...,DC=winroot,DC=analog,DC=com' under: 'DC=ad,DC=analog,DC=com'" - because Add-/Remove-ADGroupMember make the CMDLET resolve every -Members value and it resolves against the -Server the write is routed to, the GROUP's own DC, where a WINROOT DN does not exist. Both group modules now write the group's forward member attribute directly - Set-ADGroup -Identity <groupDn> -Add/-Remove @{member=<memberDn>} -Server <ServerFromDn(groupDn)> -Credential ... -ErrorAction Stop, -Confirm false kept on the removes - through one shared pure helper GroupManagementService.BuildMemberAttributeWrite (Hashtable keyed OrdinalIgnoreCase like a PowerShell literal, total by design so an unusable DN is refused by AD inside the caller's try/catch rather than thrown outside it). The attribute is the one both listings and BuildDirectMembershipFilter already read, and both write sites feed it the SAME DN variable the read-back asks about, so a successful remove is confirmable. Everything around the write is untouched: -Server routing, module credential, ErrorAction Stop, success decided ONLY by the post-write read-back (codex F10 / Known Failure Class 2), protected-principal and target-group gates ahead of the write, servicer notes, audit and notification. Primary-group caveat unchanged (a primaryGroupID membership is not on the member attribute at all, so neither form can remove it) - the three comments claiming only the cmdlet could not do it were corrected. SelfServiceGroups 1.7.0 -> 1.8.0, GroupManagement 2.7.0 -> 2.8.0, no base bump; plan Revision 2026-09-03; README untouched (it never named these cmdlets). 4 source guards re-pinned to the Set-ADGroup shape in both modules with -Server routing still asserted, +3 new helper tests; 4 probe rounds all proven biting - self-service reverted to the cmdlet form (1 fail), admin reverted (3 fails), helper key renamed to "members" (3 fails), helper comparer defaulted (1 fail))  opus subagent  reqs 111  mean-ctx 122K  est $12.02  (figures are this transcript root for 2026-09-03, which also covers the earlier IntuneSearchPerField entry at 49 reqs / $6.10, so this slice is roughly the delta; taken before this commit)
2026-09-03  GroupBulkActions-S1 (docs/GroupBulkActions-Plan.md drafted at 2e89f7a, codex openreview acceptable_with_changes with gba-1..3 folded in at 9652842, then S1: new pure Services/BulkIdentityList.cs - Parse (CR/LF/comma/semicolon split, trim, case-insensitive dedupe keeping first, 200 cap), Chunk (50), BuildBatchFilter (RFC 4515-escaped OR filter, group clause only when allowed), Match (UPN/mail/sAM for users, plus Name for groups only, exactly-one Resolved / NotFound / Ambiguous / Duplicate-of-line, total over nulls) and BulkOutcomeSummary (Success only when every row Done, counts, member lines, errorDetail); base app 2.17.0 -> 2.18.0 per gba-2, no module bump; 22 tests, 2 mutation batches 6 + 5 failures all proven biting; full suite 2242/0/3, format clean)  fable-5.1  reqs 150  mean-ctx 152K  est $22.31  (figures are this transcript root for 2026-09-03, which also covers the earlier IntuneSearchPerField and CrossDomainWrite entries at $6.10 and $12.02, so this slice plus the plan and review is roughly the delta; taken before this commit)
2026-09-03  GroupBulkActions-S2 (GroupManagement bulk remove: a checkbox per member row and a select-all that both use the single Remove button's own CanRemove predicate so primary-group and unresolved rows can never be selected; Remove selected (N) behind the ticket, one inline confirmation listing every name with the ticket, then a sequential loop through the NEW RemoveOneAsync - the single Remove button now calls the same handler - which per row re-checks GroupManagementOnPrem authorization immediately before the write (gba-1), calls the unchanged protection-gated RemoveMemberAsync, audits every branch with GUID and DN, and sends the per-member admin email only when asked; the loop asks for no per-member email and sends ONE summary email (plan D1) plus ONE GroupManagement_BulkRemoveMembers audit whose success is BulkOutcomeSummary's every-row-done rule, with requested/done/notDone/members in extra; ticket validated once per batch, pre-loop refusals audited as a failed batch with done 0; per-row outcome table Done/Not done with the service message; one member-list refresh; bulk state cleared on search, group switch, reload and Close. GroupManagement 2.8.0 -> 2.9.0. gmn-9 tripwire re-pointed at the split (1 branch in RemoveMember, 3 in RemoveOneAsync); 7 new source guards in GroupBulkActionsWiringTests, 7 mutation probes each biting (auth hoisted, hard-coded success, per-member email in loop, select-all over all rows, no ClearBulkState on switch, snapshot after await, inline second service call); full suite green, format clean. Lesson recorded: a probe restore must copy from a scratchpad backup, never git checkout - the first attempt wiped the uncommitted slice and it was re-applied)  fable-5.1  reqs 160  mean-ctx 161K  est $25.33  (figures are this transcript root for 2026-09-03 cumulative, taken before this commit; the S1 line above sits at $22.31 so this slice is roughly the delta)
2026-09-03  GroupBulkActions-S3 (GroupManagement bulk add via paste list: new GroupManagementService.ResolveBatchAsync - credentials, then the internal virtual QueryBatchCandidates seam which runs ONE Get-ADObject -LDAPFilter per 50-line chunk against the forest global catalog with the same :3268 guard as search, class-agnostic (user or group), projecting Name too (gba-3), failing the whole batch closed on HadErrors - then BulkIdentityList.Match with groups allowed; no credentials or a thrown query mark EVERY line NotAttempted with the reason, never NotFound. Page: collapsed Bulk add card with a textarea, Resolve (Parse + service batch + duplicates and over-cap lines listed with reasons, in input order), a resolution table, and Add resolved (N) behind the ticket that runs only Resolved rows through the NEW AddOneAsync - the single Add button now calls the same handler, passing its held picker DN as memberDn - per-row authorization re-check (gba-1), the unchanged AddMemberAsync with the resolution's DN, audits on every branch, per-member email only when asked; one GroupManagement_BulkAddMembers summary audit (every-row-done success) and one summary email (D1); paste and resolution cleared only on a fully Done batch; resolution cleared on group switch. GroupManagement 2.9.0 -> 2.10.0. gmn-9 add tripwire and the S5c held-DN guard re-pointed at the split; 7 new page guards + 1 service guard + 5 seamed behavioral tests (GroupBulkResolveTests); 12 mutation probes all biting - 8 page (auth hoisted, hard-coded success, per-member email, commit unresolved rows, snapshot after await, resolution survives switch, duplicates dropped, memberDn dropped), 2 service text (users-only filter, Name dropped), 2 behavioral in one rebuild (query failure as NotFound, no-credential guard removed); full suite green, format clean)  fable-5.1  reqs 167  mean-ctx 170K  est $27.87  (figures are this transcript root for 2026-09-03 cumulative, taken before this commit; the S2 line sits at $25.33 so this slice is roughly the delta)
2026-09-03  GroupBulkActions-S4 (SelfServiceGroups bulk remove: a checkbox per removable member row (CanRemove = IsRemovable with an immutable id, the single Remove's own rendering predicate) and a select-all over the same predicate; Remove selected (N) with one inline confirmation listing every name and, under each nested GROUP row, the D2 one-way warning that re-adding needs an IT Support Desk ticket; a sequential loop through the NEW RemoveOneAsync - the single Remove (user rows direct, group rows via the inline confirm) now calls the same handler - which per row re-checks SelfServiceGroups authorization immediately before the write (gba-1), calls the unchanged RemoveListedMemberAsync with the row's GUID and DN, audits every branch, sends the per-member admin email only when asked, and ALWAYS sends the affected-member notification on the existing NotifyAffectedUser predicate (a different recipient per row); one SelfServiceGroups_BulkRemoveMembers summary audit (every-row-done success, empty ticket - self-service has none) and one summary email (D1); pre-loop caller-SID and authorization refusals audited as a failed batch; per-row outcome table; one LoadMembers reload that keeps the outcome table; bulk state cleared on group switch and Back to groups, selection cleared on reload. SelfServiceGroups 1.8.0 -> 1.9.0. 8 new source guards; 8 mutation probes all biting after one guard was strengthened - the first form let the affected-user notification hide inside the sendAdminEmail block, now caught by a brace-balance check between the two ifs)  fable-5.1  reqs 175  mean-ctx 181K  est $30.34  (figures are this transcript root for 2026-09-03 cumulative, taken before this commit; the S3 line sits at $27.87 so this slice is roughly the delta)
2026-09-03  GroupBulkActions-S5 (SelfServiceGroups bulk add via paste list: new SelfServiceGroupService.ResolveBatchAsync(callerSid, kept) - SID-validated like every other entry point, credentials through a new internal virtual GetCredentialsAsync seam used only here, then the internal virtual QueryBatchCandidates seam running ONE Get-ADUser -LDAPFilter per 50-line chunk USER-only in the home domain with no -Server (the single Add's own ResolveUserMember shape and binding, so preview and commit agree), BulkIdentityList.Match with groups NOT allowed, then for the misses one batched class-bounded group probe (ProbeGroupIdentities seam, BuildGroupProbeFilter OR'd per line) that rewrites the reason to the S3 'is a group; only users can be added here' message, a probe failure keeping the generic reason; no credentials or a thrown query mark EVERY line NotAttempted. Page: collapsed Bulk add card stating the users-only rule, Resolve (Parse + service batch with the caller SID + duplicates and over-cap listed), resolution table, Add resolved (N) running only Resolved rows through the NEW ChangeOneAsync - the typed Add/Remove buttons now call the same handler (this page's typed handler covers both operations, so it is the plan's AddOneAsync) - by the resolved user's UPN (sAM fallback) so ChangeMemberAsync re-resolves exactly-one; per-row authorization re-check (gba-1), audits on every branch, admin email only when asked, affected-member notification always; one SelfServiceGroups_BulkAddMembers summary audit (every-row-done success) and one summary email (D1). SelfServiceGroups 1.9.0 -> 1.10.0. 6 new page/service guards + 7 seamed behavioral tests (SelfServiceBulkResolveTests); 13 mutation probes all biting - 7 page text, 3 service text, 3 behavioral in one rebuild (matcher allows groups, probe failure surfaces, SID guard removed); full suite green, format clean)  fable-5.1  reqs 180  mean-ctx 188K  est $32.42  (figures are this transcript root for 2026-09-03 cumulative, taken before this commit; the S4 line sits at $30.34 so this slice is roughly the delta)
2026-09-03  GroupBulkActions-S6 (README bulk bullets in both group module sections; plan Status Implemented with the section 9 traceability - every AC mapped to its slice commit, test names and probe counts, plus the three recorded implementation notes (self-service's ChangeOneAsync covers add and remove, the new GetCredentialsAsync and ProbeGroupIdentities seams, scope-specific NotFound reasons); state.md entry rewritten as IMPLEMENTED / NOT DEPLOYED with the load-bearing shape, the D1 default and how to overrule it, the probe-restore lesson, and the next steps. Whole stream in one session: plan 2e89f7a, review fold 9652842, S1-S5 568f80b edf50e4 e2bfc29 3923229 14e16f6, S6 this commit; 6 owner-facing slices, 0 owner questions asked mid-stream)  fable-5.1  reqs 181  mean-ctx 189K  est $32.86  (figures are this transcript root for 2026-09-03 cumulative, taken before this commit; the S5 line sits at $32.42 so this slice is roughly the delta)
2026-09-04  GroupBulkActions-CompactConfirm (owner finding from the first dev look: the self-service bulk-remove confirmation repeated the nested-group warning under every group row, so a handful of selections pushed the confirm button off the screen. Both pages' confirmations are now compact - Remove/Cancel on the heading line so a long selection can never scroll them away, names inline comma-joined with (Group) markers, and on self-service ONE warning line for all group rows: 'N of these are groups. Removing a group takes access away from its members, and re-adding it will require an IT Support Desk ticket.' SelfServiceGroups 1.10.0 -> 1.10.1, GroupManagement 2.10.0 -> 2.10.1; plan section 1 revision. Guard SelfService_BulkConfirm_CarriesGroupWarning re-pinned to one warning, no per-row branch, no bullets, button before names; new GroupManagement_BulkConfirm_IsCompact; probe (per-row warning and bullets restored) bites; full suite 2285/0/3, format clean)  fable-5.1  reqs 186  mean-ctx 197K  est $37.16  (figures are this transcript root 2026-09-03..04 cumulative, taken before this commit)
2026-09-04  UsageTelemetry-Plan (owner request for anonymous lightweight telemetry; fork offered - daily counters vs event rows - ruled event rows, recorded in decisions.md; one Explore subagent survey of the store/theme/event-log/navigation/retention code; plan drafted with a Home-page disclosure added mid-draft on the owner's instruction; codex openreview acceptable_with_changes - ute-1 HIGH config DB is promoted wholesale so telemetry moves to its own exchangeadmin-usage.db with no migrator step, ute-2 HIGH only LogModuleAction was hooked so the hook moves to the common WriteAuditEvent with a legacy category map, ute-3 MEDIUM session id now rides action rows via CircuitHandler.CreateInboundActivityHandler + AsyncLocal, plus first-render recording to avoid prerender double counts; three finding records, index rows, decision corrected, state entry; awaiting owner go)  fable-5.1  reqs 41  mean-ctx 244K  est $10.48  (figures are this transcript root for 2026-09-04 only, which also covers the CompactConfirm fix earlier today; taken before this commit)
2026-09-04  SharedConfigDb-Plan (owner rulings: prod promotion must stop overwriting prod's config with dev's, a prod-keeps-its-own-copy answer rejected, ONE shared exchangeadmin.db for both instances chosen - decision recorded superseding 2026-06-18 dev-wins; one Explore subagent survey (path derivation, migrator refusal, the change token nobody reads, the four TTL caches, deploy/promote/backup scripts, installer, Pester, governance text, jobs runner); plan drafted: ConfigStore:Path key, tolerant migrator with additive-only tripwire, ConfigChangeWatcher on the four caching readers, backup path resolution, promote loses copy/-SkipConfigFragments/-Refresh/DB rollback, Move-ConfigDbToShared.ps1 cutover, docs; codex openreview acceptable_with_changes - scd-1 HIGH configured path must exist (never create), scd-2 HIGH token recorded as of each load not first poll, scd-3 MEDIUM required schema includes columns, scd-4 MEDIUM local config ACL kept + pipeline messages; four finding records, index rows, state entry; awaiting owner go)  fable-5.1  reqs 82  mean-ctx 208K  est $16.29  (figures are this transcript root for 2026-09-04, which also covers CompactConfirm and UsageTelemetry-Plan earlier today; taken before this commit)
2026-09-04  SharedConfigDb-S1 (ConfigStorePath.Resolve - blank gives the content-root default, absolute local path is used and must exist, UNC or relative is fatal naming ConfigStore:Path; SqliteConnectionFactory mustExist flag - no directory creation, Mode=ReadWrite, Open() throws FileNotFoundException naming path and key on a missing file (scd-1), jobs factory unchanged; ConfigStoreMigrator accepts a NEWER database with a warning after a RequiredSchema check of every table AND column via PRAGMA table_info (scd-3), throws naming the missing table or table.column, optional ILogger, internal MigrationSteps accessor; base app 2.18.0 -> 2.19.0. Tests: ConfigStorePathTests x8, migrator newer-accepted / missing-table / missing-column / RequiredSchema-pinned-by-regex / MigrationsAreAdditiveOnly + a self-check that the tripwire catches DROP, RENAME and DROP COLUMN. 10 probes (UNC guard, relative guard, MustExist flip, blank default, factory create-anyway, old refusal restored, schema check dropped, columns unchecked, RequiredSchema column removed, tripwire weakened) all failed their target and passed on restore; lesson: Copy-Item restore keeps the backup timestamp so touch the file or the incremental build serves the mutant. Full suite 2302/0/3, format clean after a separate test-only commit fixed three pre-existing xUnit2013 lines)  fable-5.1  reqs 104  mean-ctx 212K  est $20.68  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  SharedConfigDb-S2 (ConfigChangeWatcher over IConfigStore: CurrentToken unthrottled and Unknown on failure, HasChangedSince throttled to one store read per 2 s per watcher with fail-soft false and log-once, CurrentToken refreshes the compared value without restarting the throttle so the first check after a load reaches the store; each of the four caching readers - ProtectedPrincipalService, ADAttributeEditorService allowlist+legend, PermissionValidator incl. its fail-closed exits, ExtendedLogService - records the token BEFORE its load (scd-2) and asks the watcher ahead of its TTL; ExtendedLogService.LoadLevel no longer writes the level back (a write there would make every reload trigger the next); repositories and ModuleConfigService expose CreateChangeWatcher so no reader constructor changed; IConfigStore and ConfigChangeToken remarks updated. Tests: ConfigChangeWatcherTests x8 with a counting fake store and a clock seam, per-reader ReloadsWhenTokenMoves and DetectsAChangeMadeBeforeItsFirstPoll x4 readers + legend + own-save-then-foreign-write; two existing ADAE tests re-pinned from stale-until-TTL to stale-inside-throttle-then-reloaded/fail-closed. 12 probes (throttle removed, log every failure, compare inverted, CurrentToken throttled, Unknown compared numerically, no _lastSeen refresh, seed-on-first-poll, and each reader skipping the watcher) all failed their target and passed on restore. Full suite 2320/0/3, format clean)  fable-5.1  reqs 122  mean-ctx 206K  est $23.43  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  SharedConfigDb-S3 (SqliteConfigBackup.psm1: Get-ConfigStorePathSetting + Resolve-ConfigDbPath read ConfigStore:Path from appsettings.json strict-mode/5.1-safe else <PublishPath>\config\exchangeadmin.db, -DbPath on Backup-SqliteConfigDb and Test-IsSqliteConfigDbPresent, Copy-SqliteConfigDb replaced by Copy-SqliteDbFile -SourceDbPath -DestDbPath; deploy.ps1 backs up and post-checks the resolved path and Write-Fails before any mirror when the key is set and the file is absent (scd-1), no-key keeps the nothing-to-back-up warning; promote-dev-to-prod.ps1 loses the config-copy block, -SkipConfigFragments, -Refresh and the unused -DevAppPoolName, backs up prod's resolved path with the same Write-Fail, rollback is binaries-only (robocopy /XD config) and names the verified backup instead of restoring it, dry run prints every remaining step; deploy-pipeline.ps1 help/final text stop claiming config promotion (scd-4). Pester: 5 old promote rows replaced by absence + resolved-path + scd-1 + binaries-only-rollback + dry-run rows, deploy.ps1 resolved-path/Write-Fail row, pipeline wording row, SqliteConfigBackup rows for Resolve (set/absent/blank/no file), -DbPath, Copy-SqliteDbFile x4, pure ASCII. 12 probes (Resolve ignores key, blank key returned as set, Backup ignores -DbPath, Copy keeps sidecars, deploy Write-Fail removed, deploy post-check hardcoded, promote copy reintroduced, promote Write-Fail removed, rollback mirrors config, backup via ConfigDir form, pipeline claims promotion) all failed their target; lesson: check sidecars before sqlite3 opens the copy or sqlite discards the garbage WAL itself. ScriptAnalyzer 0 errors, Pester 104/0, full suite 2320/0/3 unchanged, format clean)  fable-5.1  reqs 131  mean-ctx 210K  est $27.93  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  SharedConfigDb-S4 (tools/Move-ConfigDbToShared.ps1 - plan mode by default, -Apply acts, eight Invoke-PlanOrAction steps: DLL file-version gate against -MinimumAppVersion 2.19.0 before anything stops, stop both pools with the bulk-job warning, verified backups of BOTH databases, VACUUM INTO dev -> shared + integrity check, icacls (OI)(CI)M for both pool identities, atomic ConfigStore:Path write into both appsettings with .pre-shared backups, start both pools, 'Config store schema ready' logged once per instance; refuses UNC/relative path, existing shared file, no dev DB, split state, PlanOnly+Apply; prints the return-to-two-databases commands on failure (naming a split state) and on success; own Stop/Start helpers because deploy.ps1 and promote define theirs inline and neither exports - no shared module extracted (recorded). Install-ExchangeAdminWeb.ps1 -ConfigStorePath: key in generated appsettings, local config\ still created+ACLed, shared dir ACLed in addition (scd-4), missing shared file refused pointing at the cutover script (scd-1), byte-identical without the key. Pester: static guards for the cutover script + installer rows incl. running the real New-AppSettingsObject lifted by AST; behavioural MoveConfigDbToShared.Tests x9 in plan mode against fake publish folders with a versioned system DLL standing in for ExchangeAdminWeb.dll. 11 probes (shared ACL removed, local ACL removed, missing-file refusal removed, blank key written, key not passed, version check removed, UNC check removed, existing-file check removed, prod step 8 dropped, plan-by-default removed [static only - the behavioural run would act on real pools], split-state check removed) each failed exactly its target; lessons: Windows -Filter 'x.json.*' also matches x.json, and a PLAN line ending the buffer needs an anchored regex. ScriptAnalyzer 0 errors, Pester 125/0; no C# touched)  fable-5.1  reqs 141  mean-ctx 219K  est $31.71  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  SharedConfigDb-S5 (docs: Constitution Configuration bullets name the shared exchangeadmin.db + ConfigStore:Path and the must-exist rule, Deployment bullet gains 'promotion never copies the config database'; repo-guidance invariant 2 -> one shared DB / promotion never copies it / additive-only migrations, invariant 3 names ConfigStore:Path; README 'Shared config database' under Deploy to IIS with the cutover order and commands, a Config Store settings section, prerequisites wording; machines.md records the shared path as not yet cut over; plan Status Implemented with slice SHAs 129d091-fix be507f0 5a9c832 5776965 8657b57 + this, section 9 traceability (every AC -> commit + test names, 45 probes, final .NET 2320/0/3, Pester 125/0, ScriptAnalyzer 0 errors) and 12 deviations/notes incl. two owner FLAGS (decision entry says data_version, implementation is the change token; additive-only rule not in the Constitution); state.md entry IMPLEMENTED / NOT DEPLOYED with the exact owner-run cutover order, UsageTelemetry base bump moved to 2.20.0. Whole stream in one session: fix + S1-S5, 0 owner questions asked mid-stream)  fable-5.1  reqs 145  mean-ctx 223K  est $32.61  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  scdi-2 (ConfigStorePath.Resolve tests Path.IsPathFullyQualified instead of IsPathRooted so a drive-relative D:exchangeadmin.db is refused naming the key, UNC refusal and message kept; Install-ExchangeAdminWeb.ps1 inline check lifted into Assert-LocalAbsoluteFilePath with the 5.1-safe ^[A-Za-z]:\\ regex, Move-ConfigDbToShared.ps1 Assert-LocalAbsolutePath gains the same regex. Tests: Resolve_DriveRelative_Throws x2, Pester drive-relative rows x2 for the cutover script (behavioural) and x2 for the installer (function lifted by AST + call-site assert). 4 probes (C# IsPathRooted restored, Move regex swapped, installer regex swapped, installer call site removed) each failed exactly its 2 rows; lesson: a --no-build full suite after a probe restore runs the MUTANT - rebuild first. Full suite 2322/0/3, Pester 74/0 for the two files, ScriptAnalyzer 0 errors, format clean)  fable-5.1  reqs 177  mean-ctx 220K  est $40.77  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  scdi-3 (Move-ConfigDbToShared.ps1: Assert-SharedDbUsable runs inside the "both instances already name the shared path" branch before its return - the file must exist and pass Test-SqliteConfigDbIntegrity, else the return-to-two-databases commands are printed against the newest backups found under -BackupRoot / the newest appsettings.json.pre-shared.*.bak (placeholders when none) and Write-Fail names the path and the problem; plan mode fails the same way; the success line names the integrity check. Pester: context "both instances already name the shared path" x3 - missing file fails in default plan mode naming the path and "Nothing was changed", garbage file fails naming integrity_check, a sqlite3-created file is the only nothing-to-do; discovery-time sqlite3 resolution copied from SqliteConfigBackup.Tests. 2 probes (call removed = the shipped shape -> 2 FAIL; integrity check replaced by a no-op -> 1 FAIL) restored to 77/0. ScriptAnalyzer 0 errors, .NET suite 2322/0/3 unchanged, no C# touched)  fable-5.1  reqs 197  mean-ctx 211K  est $43.11  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  scdi-1 (ExoConnectionPool records the ExoConnectionConfig - AppId/Organization/CertificateSubject, a record struct - its runspaces connected under and compares it on every borrow against the config BorrowAsync already reads for the not-configured check; any difference drains through DrainPoolCore before the generation is read so pooled AND borrowed runspaces from the old config fail the generation test, equal values never drain (unrelated module save, identical rewrite); DrainPool records the saved config so the in-process path drains once; CreateConnected takes the compared config and a generation read BEFORE the connect (a mid-connect drain now leaves the new runspace stale - previously stamped after); internal ctor connect seam + ConfigGeneration/AvailableCount seams; base app 2.19.0 -> 2.19.1 covering scdi-2's ConfigStorePath change too. Deviation recorded: direct value compare on the existing per-borrow read instead of the ConfigChangeWatcher pre-check the record proposed. Tests: ExoConnectionPoolSharedConfigTests x8 hosted over a real temp SQLite store with a second store as the other instance and the connect step seamed. 4 probes (drain call removed -> 3 FAIL, value compare dropped -> 4 FAIL, generation stamped after connect -> 1 FAIL, DrainPool not recording -> 1 FAIL) restored to 19/19; format gate caught xUnit1051 on the seamed BorrowAsync calls - fixed with TestContext.Current.CancellationToken. Full suite 2330/0/3, format clean)  fable-5.1  reqs 212  mean-ctx 209K  est $44.81  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  scdi-docs (SharedConfigDb-Plan section 10 review log for the codex codereview dispatch over 8d9ccb0..b9e944f naming the three fixes 3f8bd0e / b481d2e / a1103cc and the recorded scdi-1 deviation; state.md SHARED CONFIG entry names the fixes and the 2.19.1 build. Whole scdi-1..3 round in one session: 3 fix commits + this, 10 probes all biting, 0 owner questions)  fable-5.1  reqs 216  mean-ctx 208K  est $45.26  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  UsageTelemetry-S1 (Services/Storage/UsageEventRepository.cs over its OWN SqliteConnectionFactory at config/exchangeadmin-usage.db - a third operational DB beside the shared config DB and the jobs DB, never promoted, never backed up, no migrator step (ute-1): idempotent CREATE TABLE IF NOT EXISTS usage_event + two indexes run once per process under a double-checked lock inside OpenReady, Insert (throws; the S2 service owns the swallow), PruneOlderThan, and three aggregates over half-open [from,to) ISO-8601 UTC text ranges - ModuleSummary grouped on COALESCE(module, category) so an action with no catalog module still reports under its raw audit category while the keyless home/access-denied opens drop out, ThemeSummary counting one row per session by ROW_NUMBER latest-theme, SessionSummary joining "acted" on the EXACT session column never a time window (ute-3); DI registration in Program.cs beside the jobs repository; base app 2.19.1 -> 2.20.0. Tests: UsageEventRepositoryTests x9 against real temp SQLite incl. Schema_HasNoIdentityColumns via PRAGMA table_info and ConfigStoreMigrator_IsUntouched pinning TargetVersion 6. 8 probes (user column added, ModuleSummary range dropped, prune cutoff dropped, WHERE rn = 1 dropped, session join replaced by the time window, success = 0 loosened to IS NOT NULL, COALESCE reduced to module, a 7th migration step appended) each failed exactly its target and restored green; lesson: line numbers taken from a mutated file are off by the lines the mutation added - re-derive them after every restore. Full suite 2348/0/3, format clean, git diff --check clean)  fable-5.1  reqs 232  mean-ctx 205K  est $47.90  (figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  UsageTelemetry-S2 (Services/UsageSession.cs - sealed per-circuit throwaway id + a static AsyncLocal<string?> Current; Services/UsageSessionCircuitHandler.cs - CreateInboundActivityHandler sets the ambient for the activity and restores the prior value in a finally; Services/UsageTelemetryService.cs - RecordSessionStart/RecordOpen/RecordTheme/RecordAction all funnel through one private Record that reads the kill switch INSIDE the try and queues through internal virtual Enqueue (Task.Run + catch-all + LogWarning) so no telemetry fault can surface to, delay or fail the operation; Enabled() defaults ON when never configured or unparseable but fail-closed OFF on a corrupt/unreadable config, logged once via Interlocked; internal static ModuleOf maps a category to a catalog id, else the legacy table MailboxPermission/CalendarPermission/Migration*, else null with the raw category still stored; AuditService gains an OPTIONAL UsageTelemetryService? third ctor parameter and calls _usage?.RecordAction as the LAST statement of WriteAuditEvent, outside the try/finally, hooked there because it is the one method every public audit method ends in (ute-2); DI in Program.cs - singleton service, scoped UsageSession + CircuitHandler. Tests: UsageTelemetryServiceTests x21 incl. an AuditService_EveryPublicAuditMethod_CallsRecordAction reflection sweep and an AuditService_HookIsLastInWriteAuditEvent source guard (brace-matched method body). 12 probes, all biting. TWO DEVIATIONS: (a) the plan's CircuitHandler_SetsAndRestoresCurrent test and its "drop the finally; FAIL" probe are UNACHIEVABLE - an async method's AsyncLocal writes never reach its caller, so the finally is provably inert from outside and the probe cannot fail; replaced with CircuitHandler_SetsCurrentForTheActivity_AndDoesNotLeakIt, proven by a synchronous-handler probe and an ambient-not-set probe. (b) the plan says "nothing else in the class changes" for AuditService, but Castle DynamicProxy binds ctors by exact arity, so the two existing Substitute.ForPartsOf<AuditService>(jsonlLog, trace) sites had to pass an explicit null. Full suite 2360/0/3, format clean, git diff --check clean)  fable-5.1  reqs ~250  mean-ctx ~205K  (estimates; figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  UsageTelemetry-S3 (Components/Layout/UsageTracker.razor - renders nothing, injects NavigationManager/ModuleCatalog/UsageSession/UsageTelemetryService/IJSRuntime; EVERYTHING happens in OnAfterRenderAsync under if (!firstRender) return - never OnInitialized, because a prerender pass would record a second session row per visit and cannot reach JS: RecordSessionStart, the current route as an open, LocationChanged subscribe, then getTheme in its own try/catch (a disconnected circuit simply leaves the theme unknown) resolved through UiThemeCatalog so the stored value is a catalog id and never free text; LocationChanged records each open; Dispose unsubscribes only if it subscribed; internal static RouteOf mirrors the ModuleVersion.razor derivation (cut at ?, trim /), internal static IsTrackedModulelessRoute keeps the table clean by recording a module-less route ONLY for home and access-denied. Placed inside <Authorized> in MainLayout so the anonymous sign-in bounce records nothing. ThemePicker records the theme AFTER InvokeVoidAsync("setTheme") so a failed set writes no row. Tests: UsageTrackerWiringTests x10 (six facts/theories) - source guards for the subscribe/unsubscribe pair, the after-first-render placement, the Authorized placement and the setTheme ordering, plus value tests for RouteOf against the spelled-out ModuleVersion derivation and for the two-route allowlist. 6 probes (unsubscribe dropped, recording moved to OnInitialized, tracker moved outside Authorized, RecordTheme moved before setTheme, query cut removed from RouteOf, allowlist widened to true) each failed exactly its target. Note: the first draft asserted DoesNotContain("OnInitialized") and failed on its own explanatory comment - narrowed to the override signatures. Full suite 2370/0/3, format clean, git diff --check clean)  fable-5.1  reqs ~275  mean-ctx ~205K  (estimates; figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  UsageTelemetry-S4 (Program.cs startup prune - PruneOlderThan(UtcNow - UsageTelemetryService.RetentionDays) in the same one-shot pass as the Message Analysis export retention but in its OWN try/catch, so a missing/locked/unwritable usage DB can neither stop the boot nor hide a failure of the pass above it, logging the count only when it removed something; ModuleCatalog AdminEventLog gains the UsageTelemetryEnabled Boolean ConfigField (DefaultValue "true", the PreventSelfGrant shape) - module version bump deliberately deferred to S5 per the plan, which lands the operator-visible change; Home.razor Important Notice gains the disclosure bullet gated on @if (Usage.Enabled()) - the same read the recorder uses, so the notice cannot claim collection that is not happening - with the retention figure interpolated from @UsageTelemetryService.RetentionDays rather than typed twice. Tests: Home_DisclosesTelemetry_OnlyWhenEnabled (gate before the phrase, phrase exactly once, constant not a literal 90) and KillSwitch_IsABooleanConfigFieldOnTheEventLogModule (both sides addressed through UsageTelemetryService.ConfigModuleId/ConfigKey so a rename cannot half-land). 4 probes (disclosure ungated, retention hardcoded to 90, field deleted, DefaultValue flipped to false) each failed exactly its target. NOT COVERED BY A TEST, deliberately: the startup prune block itself - it is inside Program.cs top-level statements with no seam, and the plan's test table lists none; the plan's manual check 4 covers it. Format gate caught xUnit2031 (Where before Assert.Single). Full suite 2372/0/3, format clean, git diff --check clean)  fable-5.1  reqs ~300  mean-ctx ~205K  (estimates; figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  UsageTelemetry-S5 (Components/Pages/AdminEventLog.razor gains an Events/Usage two-button toggle above the filter row - pure C# state, no Bootstrap JS; showUsage hides the events table, the undo side panel (its top-level @if is now `!showUsage && undoPreview != null`, not just a CSS hide) and every filter control except the date range, which is wrapped so the two date inputs stay and now call OnDateRangeChanged - the range drives whichever view is showing instead of silently reloading events behind the usage tables. LoadUsage runs on toggle and on date change only, never on OnInitialized, converts the local DateOnly range to a half-open UTC [from, to) and reads the three repository aggregates; a query fault CLEARS all three before it sets usageError, so a stale aggregate can never sit next to an error message. internal static BuildModuleRows(IReadOnlyList<ModuleUsage>, IReadOnlyList<AdminModuleDescriptor>) is the whole logic and is pure: catalog order preserved, every module gets a row with zeros when unopened (coverage, not a top list), each aggregate key consumed as it is matched so the leftovers - audited categories that map to no module, e.g. Lookup - are appended by name and badged rather than dropped, and ActionsPerOpen guards Opens == 0. Themes render through UiThemeCatalog.Resolve(...).Name so a retired id still reads as a name. Same EventLog policy, no new permission, no new injectable beyond UsageEventRepository + ModuleCatalog. AdminEventLog 1.1.0 -> 1.2.0. Test: BuildModuleRows_FillsZerosForUnopenedModules, reading the module count from the catalog rather than writing it down. 3 probes (zero-fill dropped, ActionsPerOpen guard removed -> NaN, leftover loop deleted) each failed exactly its target and restored green. Full suite 2373/0/3, format clean, git diff --check clean)  fable-5.1  reqs ~325  mean-ctx ~205K  (estimates; figures are cumulative for this transcript root today, taken before this commit)
2026-09-04  UsageTelemetry-S6 (docs close-out, no code: README gains a "Usage telemetry" paragraph under the Admin Event Log page saying what IS recorded (throwaway per-visit id, timestamp, route/module opened, audit category + action name + success flag, theme id) and what is NOT (user, IP, target, ticket, any operator-typed text), the 90-day startup prune, the separate never-promoted never-backed-up database, and where the kill switch lives; .agents/repo-guidance.md Architectural Invariant 3 now names BOTH exchangeadmin-jobs.db and exchangeadmin-usage.db as per-instance runtime files deliberately outside the backup and the promotion copy, so the next deploy change does not "fix" their absence; docs/UsageTelemetry-Plan.md Status Draft -> Implemented with the six slice SHAs, the drafted-vs-actual base bump corrected (2.18.0 -> 2.19.0 as written, 2.19.1 -> 2.20.0 as landed, SharedConfigDb having taken 2.19.x first), section 9 filled with an AC1-AC13 traceability table, the per-slice and final figures (2373/0/3 from 2339; 33 probes: 8/12/6/4/3) and SEVEN recorded deviations - the COALESCE grouping key, the extra Category member, the unachievable AsyncLocal restore probe, the Castle-DynamicProxy arity edit to two existing test files, the untestable startup prune plus the unplanned kill-switch field test, the raw-category rows in place of the plan literal Lookup/Admin Settings/Other buckets, and the undo-panel hide + date-input rebinding - and section 10 an implementation-log entry; state.md entry flipped to IMPLEMENTED, NOT DEPLOYED with the next step being the owner-run dev deploy and an owner-dispatched codereview over e33b201..HEAD. Docs-only: git diff --check only, no build/test/analyzer run and none needed)  fable-5.1  reqs ~340  mean-ctx ~205K  (estimates; figures are cumulative for this transcript root today, taken before this commit)
- 2026-09-08 save-then-prompt bug (owner-reported): render between dirty-clear and forced reload, 4 sites across ModuleConfig/AdminSettings + shared MethodBody helper + 4-case guard theory; one session, opus.
- 2026-09-08 cutover script app-pool identity read (outage fix): Get-Item -> Get-ItemProperty in Get-AppPoolIdentity, read hoisted to step 1b before anything stops, plan mode attempts it and warns when it cannot, catch block restarts both pools when nothing was repointed; 6 Pester cases, 5 bite under the Get-Item mutation; one session, opus.
- 2026-09-08 ServiceHealth module (new module, port of D:\source\servicehealthmonitor Flask dashboard): ServiceHealthService (singleton, 600s cache under SemaphoreSlim with double-check, two Graph reads, posts parsed inline off the issues list, HtmlSanitizer 9.2.1039 allow-list with target/rel forced on anchors), ServiceHealth.razor (MarkupString only on service-sanitized fields), catalog descriptor 1.0.0 (no base app bump - new module), 41 service tests + 5 page source guards + 5 catalog tests; sanitizer mutation probe (Sanitize -> passthrough) failed 14 of 40. Full suite 2440/0/3, format clean, git diff --check clean; one session, opus.
- 2026-09-08 ServiceHealth 1.1.0 parity fix (owner rejected 1.0.0 as "a dumb, long text list" with no service-to-incident link): IssuesFor/IssueCountFor on ServiceHealthStatus, StatusWords short-word map from the original's getStatusText with the camel split kept as fallback, ParseIncident gains featureGroup/origin/isResolved/endDateTime and the details blocks (values are HTML - sanitized, ValueHtml added to the page MarkupString allow-list), page rebuilt around expand-in-place service rows that drive the incident list plus the original's two filters, one shared RenderFragment for both incident call sites, no JS added; +25 tests (filter projectors are real behaviour, page wiring still source-text only). IssuesFor mutation probe bit 3. Full suite 2465/0/3, format clean, git diff --check clean; same session as 1.0.0, opus.
- 2026-09-08 ServiceHealth 1.2.0 owner design ruling ("add sort and filter for services, expand details when the service card is clicked, do not dump an unsorted list of TLDRs under a vague open incidents and advisories section, default to sorting by service status then alpha, filter *"): flat incident list deleted (one IncidentCard call site now, pinned by a test), SortServices/StatusRank projectors with worst-first-then-alpha default plus Open incidents and Name modes, MatchesName wildcard filter replacing the service dropdown (empty or * = all, plain word = contains, */? = anchored), status dropdown gains "Has open incidents", opening a service opens its incidents already expanded, Expand/Collapse all over the filtered set. +13 net tests. Mutation probe (flatten StatusRank + drop the regex anchors) bit exactly 3. Full suite 2478/0/3, format clean, git diff --check clean; same session, opus.
- 2026-09-08 ServiceHealth 1.3.0 owner design ruling ("just make it look like the fucking original since you are incapable of improving it", after five rejected design rounds and an explicit retraction of the earlier donut-chart praise): new scoped stylesheet Components/Pages/ServiceHealth.razor.css (first .razor.css under Components/Pages/) reproducing servicehealthmonitor/static/style.css geometry with every literal hex mapped onto a --ui-* theme token except #4fc3f7 (pinned by a test), page rewritten to the original's structure (gradient header, four summary cards, original filter row plus the retained sort dropdown, 300px service-card grid with status-coloured left borders, "Current Issues" section restored below and filtered by the clicked card), MatchesName deleted in favour of the original's service select so FilterServices matches the id exactly, ActiveIssueCount adopts the original's !endDateTime definition (closes the 17-vs-15 discrepancy at 15). 11 mutation probes, all biting; one of them caught a vacuous test (ActiveIssueCount's fixture gave both definitions the same answer - fixture fixed). Full suite 2478/0/3, format clean, git diff --check clean; one session, opus. Cost note: a probe script that restored with `git checkout --` wiped the uncommitted page rewrite; recovered from the session transcript. Probe scripts must snapshot files, never restore from git, while a slice is uncommitted.
- 2026-09-09 ServiceHealth 1.3.1 owner ruling, in full: "no gradients". One declaration deleted from Components/Pages/ServiceHealth.razor.css (the header band's linear-gradient); the band takes the flat var(--ui-brand) fill that was already under it, so geometry, brand colour and all ten themes are unchanged. New guard Styles_CarryNoGradient asserts the stylesheet contains no "gradient" at all, so nothing reintroduces one in a card or hover state; probed by restoring the gradient - the guard failed, restored (snapshot copy + touch to defeat the MSBuild timestamp skip), 31/31 page tests green. Five stale prose references to "the brand gradient" reworded to "the brand-filled header band" across the test comment, docs/ServiceHealth-Plan.md, .agents/decisions.md and .agents/state.md; the #4fc3f7 header-icon literal still has no token and is still the only literal the style guard allows. Module 1.3.0 -> 1.3.1, base app unchanged at 2.20.2. Full suite 2479/0/3, format clean, git diff --check clean, no non-ASCII; one session, opus (continued after a compaction).
- 2026-09-10 CloudPasswordReset plan revision 4 (docs only, no code): codex openreview intake over c493b2a..7c47c3c ("Acceptable with changes", 3 findings). cpr-1 (operator is the derived owner) DECLINED on the owner's challenge - no escalation when the operator already holds the account; .agents/review/cpr-1.contested.md, and the plan's self-reset section now carries the reasoning so it is not re-raised. cpr-2 (ungated delivery-failure reveal) UPHELD - owner ruling "if the send itself fails, then fail closed": password discarded not displayed on any tier, CloudPasswordReset_DeliveryFailed audited, downstream mail delivery declared out of scope; decisions.md entry. cpr-3 (corroboration reads AD fields the query never requests) UPHELD and verified - three missing fields not one (GivenName, Surname, Enabled), folded into the derivation section and S1 with a test requirement. Review log filled in, index rows added, state.md NEXT refreshed to D1/D2 then S0. One session, opus, continued after a compaction.
- 2026-09-10 CloudPasswordReset plan revision 5 (docs only, no code): owner ruling "d1 app chooses. there's a password generator in D:\source\pwgen that will serve as the model. use the algorithm it's using, not the code." Read pwgen/src/main.rs in full (905 lines) and measured its wordlist (7,771 words, 3-9 chars, four hyphenated, length histogram recorded) rather than guessing at the method; wrote a new "The generated password" section specifying the algorithm in prose - target length, entropy-bounded word count, length-fitted word selection with shuffled chosen lengths, balanced capitalisation with no adjacent duplicates, per-gap separators, padding distributed across every slot, effective-pool entropy check, refuse-never-degrade - plus the two C# adaptations (RandomNumberGenerator only, wordlist as embedded resource). D1 marked SETTLED; the D2 section deleted entirely and its content restated as a requirement in the Graph surface (tenant-wide reach including Global Administrators is the point of the module, with a live-verification test rather than an open question). New slice S2 for the generator with provenance/licence step, everything after shifted by one (base bump now S4); AC13-AC16 and two manual verification items added. decisions.md entry for D1, state.md rewritten, index unchanged. One session, opus, continued after a compaction.
- 2026-09-10 CloudPasswordReset plan revision 6 (docs only, no code): two owner corrections against the exec summary. (1) forceChangePasswordNextSignIn true -> false, hard-coded with a pinning test - "that disallows signin in too many instances"; the generated password is now the durable password, so the generator's strength became load-bearing, the owner email must not promise a change prompt, and manual verification item 2 inverted to assert no interrupt on a path that cannot service one. (2) The display-name-only rule for the destination reversed after the owner asked it be justified and it could not be - the address is not a secret and showing it is the only human check on a mis-resolved owner; the actual control (derived, read-only, never editable, never chosen from a list) was always a different control and is untouched. AC4 rewritten, AC17 added, decisions entry, state.md. One session, opus.
- 2026-09-10 CloudPasswordReset plan revision 7 (docs only, no code): owner reversed my second hard-code - change-at-next-sign-in is neither always-on nor always-off but an operator checkbox "just like it is in the MS portal", default unchecked. Recorded the general rule it comes from (the app gives L2 the console's capabilities inside an audited credential-free interface, so withhold credentials and enforce audit, never withhold controls) in decisions.md and state.md, since it decides this whole class of question. The flag now threads page -> service -> PATCH body -> both emails: S3 takes it as a parameter never read from config, S4's email helper takes it so the closing line matches reality, S5 renders the checkbox with help text about accounts that cannot service a change prompt, S6 carries it into extra and the admin mail with a test that follows one value the whole way. AC17 rewritten, manual verification item 2 now covers both states. One session, opus.
- 2026-09-10 CloudPasswordReset plan revision 8 (docs only, no code): owner "these logs are going to splunk, so they need to be explicit and clear." Verified first rather than assumed - no "splunk" reference existed anywhere in the repo, AuditService.WriteAuditEvent (:392-411) already emits one JSON object per event and MergeExtra (:38-45) copies nulls through, so explicit-null fields survive to the writer and no transport change is needed. New plan section "Audit fields, and Splunk" fixing the module's extra field set as a published interface: a 12-row table (targetObjectId, targetCloudOnly, targetDirectoryRoles as an always-present array, ownerResolution, ownerSam, ownerMail, forceChangePasswordNextSignIn, passwordDelivery, revealUsed, refusalReason with a closed 15-value list, protectedPrincipalServiced) plus six rules - one fact per field, JSON booleans, closed enumerations, every field on every event with explicit null, names frozen once shipped, nothing derived from the password. Named IntuneDevices.razor:1414's packed wipeFlags string as the precedent not to copy, and fenced the cleanup of existing modules off as a separate stream so this does not license a sweep. AC18 added; cross-reference into the Audit and notification bullet. decisions.md entry, state.md. One session, opus, continued after a compaction.
- 2026-09-11 CloudPasswordReset S0 landed (first code of the stream, PowerShell only): owner said "go", which per the plan's own text authorizes S0 and nothing after it. Wrote `tools/CloudAccountOwnerDerivation.psm1` (three pure functions: candidate keys, name corroboration, fail-closed outcome collapse - no AD/Graph/network/filesystem, the model for `Services/CloudAccountOwnerResolver.cs` in S1), `tools/Get-CloudAccountOwnerCoverage.ps1` (the read-only survey, `-PlanOnly`-shaped, Graph + AD reads, one CSV, never PATCHes) and `tests/ps/CloudAccountOwnerDerivation.Tests.ps1` (40 tests). Read `ExecuteValidateExists` in full rather than trusting the plan's summary of it, which surfaced a finding the plan did not record: the USER query carries no `-Server`, so it binds the local domain only (the `-Server` routing at `:355` is scoped to Group, `ResolveGlobalCatalog` only at `:660`), meaning a cross-domain sAM collision is invisible to the module and corroboration is the sole guard. Built it into the survey as a measured `ForestMatchCount` column rather than silently changing the design. Added `UnresolvedOwnerDisabled` as its own outcome class because the plan names Enabled without stating the leaver rule's shape. The new tests bit on first run: 4 failures exposing a real defect - `Get-CloudAccountOwnerCandidate` returned a bare string for a single candidate because PowerShell unrolls one-element arrays, so any caller indexing the result got a character; fixed with a unary comma. ASCII lint clean, PSScriptAnalyzer no Errors (CI's gate), Pester 180/180 across all 8 files. Plan Status flipped Draft -> In progress with an S0 findings section; decisions.md entry for the go and the finding; state.md. One session, opus, continued after a compaction.
- 2026-09-11 CloudPasswordReset: local-domain finding settled by owner topology (forest root/user domain); downgraded across plan, state and decisions. Records only, no code change.
- 2026-09-11 CloudPasswordReset: owner rejected the topology-based closure of the local-domain finding ("you cannot hard-code any ADI-specific ANYTHING into ANYWHERE in this app") - the assumption was in the justification, not in any source line, and that is exactly what was rejected. Reverted the settle across plan/state/decisions and re-stated the finding as a binding S1 requirement: resolve the owner against the global catalog discovered at runtime from the host forest, refuse as Ambiguous on 2+ matches anywhere in the forest, no rule resting on a domain being mailbox-free or on host domain membership. New app-wide decisions entry (environment neutrality, four testable clauses) and a new Architectural Invariant 7 in repo-guidance. Consequence flagged: S0s survey must make the forest-wide result its primary column before it is run, else it measures code that will not ship. Records only in this commit.
- 2026-09-11 CloudPasswordReset: owner rejected BOTH the topology-based closure and my forest-wide replacement - the estate has several domains and trusts, most irrelevant to Entra, so a forest-wide sweep queries the wrong directories and manufactures collisions. Settled design: module config gains a Search Domains checkbox list enumerated at runtime from the forest and its trusts (no name hard-coded or typed), the lookup searches exactly the checked domains, 2+ matches across that set is Ambiguous and refuses, an in-app note says how to choose, nothing checked or unreadable refuses per the standing fail-closed rule. Owner: "stop wrapping the admins in bubble-wrap." Recorded the general principle too - an operator setting with a clear explanation is the right answer where I reached for an automatic one; withhold credentials and enforce audit, never withhold the decision. Plan, state, decisions (new entry + correction to the reopened-finding block). Records only.
- 2026-09-11 CloudPasswordReset S0 derivation rebuilt for three-source agreement (owner go "go", scope: the two tool files and their tests; the S0 re-run is NOT covered - "I approved ONE"). The superseded design matched on the whole UPN, which is why the owner's own account missed: cloud key "michael.coelho", on-prem sAMAccountName "mcoelho", on-prem UPN "michael.coelho@<domain>". Source 3 now compares the UPN LOCAL PART to a local part (userPrincipalName=<key>@*, proxyAddresses=smtp:<key>@*, sAMAccountName=<key>), source 1 is employeeId OR'd against employeeID/employeeNumber/extensionAttribute1, source 2 is the display name with a trailing -CLD/_CLD stripped. employeeType is NOT a fourth id attribute (it holds CWK, a worker class, a category not a person); manager and otherMails stay struck. The rule that carries the safety property is an asymmetry: an IDENTIFIER arm matching more than one user is DISCARDED as non-evidence and the other sources still decide, while a PERSON arm matching more than one REFUSES as AmbiguousWithinSource, because two people sharing a display name are two real candidate people. Cost falls from 2 x domains queries per candidate key to ONE OR'd query per domain, agreement computed in memory. Tests rebuilt 40 -> 74; suite 214/0; PSScriptAnalyzer no Errors. Three mutation probes, all BITE: invert the id-arm discard, revert source 3 to a whole-UPN arm, drop Get-ObjectPropertyValue from Export-ModuleMember. That last one is a new test class worth keeping: Invoke-PlanOrAction never runs its scriptblock in -PlanOnly, so a script calling an UNEXPORTED module helper parses clean, plans clean and dies on the first real account - the module had exactly that bug. Cost paid twice to the same trap: "return ,$array" reaches the caller as ONE pipeline object, so @(f) and "f | Should" re-nest it; that produced the false 0% in 8feb993 and 12 test failures here, and is now documented in the module, a source-text tripwire and a calling-convention docblock. Also self-inflicted: my throwaway probe script used -like against needles containing [ ], let Set-StrictMode leak into Pester, and aborted mid-probe with the module mutated on disk. One session, opus, continued after a compaction.
- 2026-09-11 CloudPasswordReset: the derivation is dead, plan rewritten (docs only, no code). The one approved S0 run returned 46.5% owner coverage (80 of 172 in-scope privileged cloud-only accounts) and all 84 misses returned ZERO directory rows, not wrong people - the accounts simply have no on-prem object findable by any of the three sources. Split by naming era: "Last, First" 68/79, "First Last" 12/66, single-token 0/19, and every one of the 31 underscore-UPN accounts missed. Two mechanical fixes (underscore-to-dot separator normalization, flipped display-name order) would have reached ~84%, not 100%. employeeId is populated on 0 of 172 cloud accounts, so source 1 contributed nothing and the three-source design ran as two; the owner refused to populate it ("we're not going to change several hundred cld accounts"). Owner ruling: "if we cannot get a 100% working match, then matching is off the table" - the operator now types the destination address, and "destination email needs to be in the logs and in the admin alert email". Deleted the whole derivation apparatus from the plan (~380 lines), collapsed three audit fields to one required destinationAddress, rewrote AC3/AC4/AC5, added the D4 gate (does the reveal permission still fence anything, now that the operator can reach the password by addressing the mail to themselves - recommendation: keep it, stop calling it a security boundary), and compressed the state.md block from ~208 lines to ~60. The judgement call worth recording: I refused to let the plan quietly keep its old safety language. The superseded claim - that the operator never learns the password, so resetting a Global Admin is a nuisance rather than a takeover - is FALSE under the new design, so three passages now state in plain words that prevention was traded for detection (section access + audit + admin alert), that the owner made the trade knowingly, and that the phone-call process it replaces has neither property. A reviewer finding the old sentences in git history would otherwise read this as a regression. One session, opus, continued after a compaction.
- 2026-09-14 CloudPasswordReset: owner stopped the module - "stop this module's development and put it on hold" - immediately after D4 was put and answered "no. neither." Records only, no code. Plan Status In progress -> ON HOLD with a resumption note (starts at S2, nothing half-built, re-put D4 before S5); D4 re-headed "PUT, NOT ANSWERED" with its analysis kept intact because the analysis is still correct and only its status changed; state.md block cut to a hold entry; new decisions.md entry. Two judgement calls worth recording: (1) no reason for the hold was given and I did not invent one - a fabricated rationale in a durable record is read by the next agent as the owner's, so the entry says plainly that no reason was given and none is inferred; (2) D4 is recorded as LAPSED, not settled, and the plan states the recommendation is a recommendation and not an approval, because "no. neither." rejects both options without choosing anything, and a later reader finding a recommendation next to a closed question would implement it. Flagged the one item that outlives the hold: the three survey CSVs are untracked, gitignored and still on disk, naming every in-scope account plus matched owner sAMAccountNames/emails/DNs, and now have no purpose - awaiting an owner decision to shred. Also still open and not module work: the survey app registration holds four ReadWrite grants it never needed. One session, opus.

- 2026-09-14 drift sweep: superseded state descriptions archived verbatim; live manual checks and the CloudPasswordReset hold retained. Re-verified both deployment appsettings/DLL metadata, shared module configuration, disabled AccountLockout and local tooling. Archived settled decision rationale behind operative rule pointers; flagged SQLite package availability, record conflicts and survey inventory for judgment. Records only; git diff --check is the verification entry point; no app tests or live acceptance checks run.
- 2026-09-14 CloudPasswordReset runtime investigation: owner clarified an L2-only employee-CLD workflow and supplied the M365Connections/PTK authentication path. Read current scoped AD/Graph data and exercised candidate rules in memory; proposed design and validation limits are in `.agents/research/cloud-password-owner-runtime.md`, with the private receipt listed in `.agents/machines.md`. No product implementation or directory writes. Records-only verification: git diff --check. One Codex session; token usage not measured.
- 2026-09-15 Alphabetical module ordering S1-S4 (docs/AlphabeticalModuleOrdering-Plan.md steps 1-4): new Modules/ModuleCategories.cs with the five nav categories as constants and all 28 descriptors plus NavMenu PrimaryCategoryOrder pointed at them; the sidebar split moved off the SortOrder >= 900 threshold onto ModuleCategories.Administration (the two selections were verified identical first, and a catalog test now pins the Administration set); GetOrdered() and GetConfigurablePolicyAliases() reordered to DisplayName with StringComparer.OrdinalIgnoreCase, with NavMenu sorting within a section on DisplayName explicitly rather than leaning on OrderBy stability; then the SortOrder field deleted from AdminModuleDescriptor, from all 28 descriptors and from the stale here-string fixture in tests/ps/ValidatorChecks.Tests.ps1 that the compiler cannot see. Four new tests, each proven non-vacuous by mutation: category-is-a-constant (typo category), Administration-set (a module joining the block), display-name order and alias-list order (both reverted to SortOrder), and the alias-set guard (renamed one alias) - section access is keyed on policy alias, so the whole point is that reordering moves no alias. Step 5 (contract docs and the 2.20.2 -> 2.21.0 bump) is deliberately NOT in these four commits; a separate agent owns it, so the app version and docs/AdminModuleSpec.md still describe SortOrder until that lands. Verified per commit: build Release, dotnet test on the .slnx (2484 passed / 0 failed / 3 skipped at S3), dotnet format --verify-no-changes, git diff --check; S4 additionally ran PSScriptAnalyzer and Pester. One session, opus-5 implementation subagent; token usage not measured.
- 2026-09-15 Alphabetical module ordering S5 (docs/AlphabeticalModuleOrdering-Plan.md step 5, the last step): the two binding contract docs brought into line with the code the previous four commits left behind, then the base app bump 2.20.2 -> 2.21.0 (VersionPrefix, AssemblyVersion, FileVersion; no module Version moves, because no module behavior changed). The plan named four line numbers; reading both documents end to end found more than four places that still taught the old model, so the docs commit also adds an Category And Ordering subsection to AdminModuleSpec.md, rewrites its UI Rendering NavMenu bullet from "sorted by SortOrder" to category grouping plus alphabetical DisplayName with the Administration bottom-block split, and in AdminModuleDeveloperGuide.md adds two Descriptor Rules bullets (Category names a ModuleCategories constant; there is no ordering field) and rewrites Categories And Navigation, whose list of six "established categories" was the quietest trap in either document: it still offered Other, which is the property default and now fails Catalog_EveryCategoryIsAModuleCategoriesConstant, so an author following the guide would have written a descriptor that compiles and breaks the suite. Left deliberately: the verbatim AdminModuleDescriptor listing keeps its = "Other" default because it is a true copy of the record, with the new rule bullet next to it saying the default is not a usable value. Spec version header left at 2.0 - git history shows it is the document version, never the app version (1.0 -> 1.1 -> 2.0, with the app version carried separately in a "based on/verified against" stamp), so there is no drift to fix. Verified: build Release, dotnet test on the .slnx 2484 passed / 0 failed / 3 skipped, dotnet format --verify-no-changes clean, git diff --check clean; no PowerShell touched, so no PSScriptAnalyzer or Pester run. Noticed, not fixed, both out of scope: the guide header still says "Host baseline: ExchangeAdminWeb 2.3.22", and its Run before submitting block runs dotnet format against the csproj rather than the .slnx the CI gate uses. One session, opus-5 subagent; token usage not measured.
- 2026-09-17 Migration stale open report (docs/MigrationStaleReport-Plan.md, owner-reported issue 3 of the 2026-09-15 queue, steps 1-6 in one slice): the state.md hypothesis was wrong and is now falsified - the report was never keyed on batch name, so the guessed fix would not have touched the reported repro, which reuses the name. Actual cause: userReport holds a one-time snapshot written only by LoadUserReport and cleared only by the Close button, the render guard at Migration.razor:771 keys on the email address ALONE, and seven separate paths reload or discard batchUsers without touching it. One CloseUserReport() helper now owns the clear and is called at all seven (LoadMigrationStatus, both ToggleBatchDetails branches, ExecuteUserAction's success reload, RefreshBatchUsers, both SearchUser paths), the Close button calls it instead of an inline two-field lambda, and a reportGeneration counter closes the race a naive clear leaves open - a slow Get-MigrationUserStatistics -IncludeReport landing after the clear would otherwise repopulate the panel the reload just emptied. No lock: a Blazor circuit runs its handlers on one synchronization context. Service untouched; it fetches when asked. Module version 1.8.0 -> 1.8.1, no base app bump (module-scoped behavior). Ten tripwires added, anchored per BRANCH not per method body - the discard guard matches each `batchUsers = null;` occurrence against the statement that follows it, so a method with one guarded branch and one unguarded branch fails. Guard proof, all three bit: deleting CloseUserReport from only the COLLAPSE branch of ToggleBatchDetails failed two tests while the expand branch stayed correct; removing the generation check failed the ordering guard; removing reportGeneration++ failed the helper guard. Restore verified byte-identical and the file touched afterwards, per the mtime trap. Verified: build Release 0 errors, dotnet test 2494 passed / 0 failed / 3 skipped, dotnet format --verify-no-changes exit 0, git diff --check exit 0; no PowerShell touched so no PSScriptAnalyzer or Pester. Not done and needing an owner go: the manual acceptance checklist (no bUnit harness exists, so nothing here reaches the rendered page) and the implementation codereview. One session, opus-5, continued after a compaction; token usage not measured.
- 2026-09-17 Migration stale open report, implementation codereview (docs/MigrationStaleReport-Plan.md): codex at @azure-openai-eus2-global/gpt-5.5-dzs xhigh, standard tier, read-only, over 1250220..3e4f13d; capability proof passed (quoted Migration.razor:778 verbatim and echoed three real git log subjects). Verdict findings (1). The prompt named five specific questions rather than a bare defect hunt, which is what made the clean half of the result usable: four came back with nothing and now stand as reviewed rather than unexamined - no eighth path reloading batchUsers, the generation guard correct on the success, catch and finally continuations, no field combination rendering a half-populated panel, and the module-only 1.8.1 bump correct against the Constitution rule. The fifth question - can a broken page still satisfy the ten tripwires - produced msr-1 (MEDIUM, admitted after confirming the mechanism line by line, not on the reviewer word): the tripwires assert CloseUserReport() precedes the refetch, and RefreshBatchUsers does exactly that, bumping the generation BEFORE its await and never nulling batchUsers, so the old rows keep rendering for the whole Exchange call with a Report button gated on neither loadingBatchUsers nor actionInProgress (Migration.razor:737-739, unlike the refresh button beside it at :750 and every action button at :706-727). A report opened in that window captures the post-bump generation and writes itself over rows since replaced. ExecuteUserAction has the same shape and a narrower window; the other five sites are safe because they null batchUsers or move expandedBatch. No code authorized - the fix is a follow-up slice needing a go. Records only: finding doc, index row, plan Implementation review section, state.md. Dispatch scratch is gitignored per the 2026-08-07 index note. One session, opus-5, continued after a compaction; token usage not measured.
- 2026-09-17 msr-1 fix, migration report vs in-flight row reload (docs/MigrationStaleReport-Plan.md): the owner said "fix it" without naming an option, so the choice was mine. Took the structural route rather than the two-site patch the reviewer suggested: one private ReplaceBatchUsers helper owns every replacement of the rendered rows and closes the report itself, and all seven assignment sites hand it the awaited fetch directly, which is what puts the close after the await. Rejected gating the Report button on loadingBatchUsers - one attribute, but it blocks a read-only diagnostic and leaves the invariant unenforced. The useful lesson is about the tripwires, not the code: the original ten asserted the close precedes the refetch, which the defective code did, so they were satisfied by the bug. The replacement assertion is per occurrence and negative - no path assigns batchUsers outside the helper - and it is the only one an eighth reload path cannot slip past. Guard proof: three mutations, all bite, restore byte-identical; mutation 1 is literally the pre-fix code and the original ten passed it. 52 MigrationStatusPageTests green, full suite green, format clean. Module 1.8.1 -> 1.8.2, no base app bump. One session, opus-5, continued through two compactions; token usage not measured.
- 2026-09-17 button-gating plan + prerequisite fix (docs/MigrationButtonGating-Plan.md): owner ruled no button may be clickable unless the click will definitively execute. Planned one page-level in-flight predicate every control consults. Dispatched codex/gpt-5.5-dzs xhigh to review the PLAN before writing code - cheapest review in the queue so far and the highest-yield: it caught that pendingActionLabel in the predicate would disable Confirm at the only moment it renders, which would have shipped a page where no destructive action could ever be confirmed, with all three proposed tests green. Lesson: review the plan, not just the diff; a design flaw that the tests are also blind to cannot be caught downstream. All seven findings verified against the file before acceptance rather than taken on the reviewer word; all seven were real. Commit B fixes the live instance the review found: SearchUser cleared loadingBatchUsers after each await while its finally cleared only isSearching, so a thrown Exchange call left it stuck for the life of the circuit. Guard proof: mutation reinstating the pre-fix shape fails exactly the new test, 1 failed / 52 passed, restore byte-identical. Module 1.8.2 -> 1.8.3. One session, opus-5.
- 2026-09-17 button gate (docs/MigrationButtonGating-Plan.md commit 2): one IsBusy predicate over eight in-flight flags, consulted by 27 of 31 buttons plus the three tab anchors; four exemptions named with reasons in the tests. Seven tripwires, not the planned six - the seventh enforces the tab handler guard the plan left unenforced. Two of the seven failed on first run against CORRECT code: the scanners matched the words "await" and "finally" inside the new explanatory comments, so a StripLineComments pass is now mandatory in any source scanner here. Guard proof on the three representative controls (Delete, Show report, Details): each mutation fails exactly EveryButtonConsultsTheBusyPredicate, 1 failed / 59 passed, restores byte-identical, restored run 60/60. Full verification green: build Release 0 errors and the same 23 warnings as before, 2505 passed / 3 skipped, format clean, diff --check clean. Module 1.8.3 -> 1.9.0. One session, opus-5, resumed once through compaction.
- 2026-09-18 Service Health load feedback (docs/ServiceHealthLoadFeedback-Plan.md, owner-reported queue item 7, steps 1-5 in one slice): the complaint reads as "add a spinner" and state.md correctly warned it is not - the page already had three correct spinners. Root cause found by reading, not guessing: the page prerenders (Program.cs:374 AddInteractiveServerRenderMode, no prerender:false anywhere in the app), prerendering emits no HTML until OnInitializedAsync reaches quiescence, and OnInitializedAsync ended with await LoadAsync, so the Delinea read, the Graph token and both Graph collections all completed before the browser received its first byte - the spinners were unreachable by construction. The operator-visible half is worse and is what produced the repeated clicks: enhanced navigation (App.razor subscribes to blazor:enhancedload) leaves the PREVIOUS page fully rendered and interactive during the fetch, so there is no visual change at all. Owner chose shape B, the BlockedSenders.razor:166-181 precedent, over disabling prerender. Also resolved a question state.md had flagged unverified: the authorization round trip is NOT part of the problem - GroupAuthorizationHandler.HandleRequirementAsync is synchronous over in-memory state and returns Task.CompletedTask, no I/O - which is why the auth check stays in OnInitializedAsync in both the precedent and here. Step 3 matters more than it looks: Blazor does not auto-render after OnAfterRenderAsync, so without StateHasChanged in LoadAsync's finally the deferred load leaves a permanent spinner, which is worse than the bug. Step 4 fetches the two Graph collections under Task.WhenAll instead of serially; no existing test asserted the second call is skipped when the first fails, so the failure-path change is compatible. Five tripwires, every one stripping comments first - the migration work's lesson applied pre-emptively, and it mattered, because this change's own comments name OnAfterRenderAsync, LoadAsync and StateHasChanged. Guard proof: steps 1, 2 and 3 mutated back independently, each 1 failed / 34 passed against its named tripwire, all three restores byte-identical by SHA256 with the mtime touched. Verified: build Release 0 errors and the same 23 pre-existing warnings, dotnet test 2510 passed / 0 failed / 3 skipped, format clean, git diff --check clean; no PowerShell touched. Module 1.3.1 -> 1.3.2, no base app bump. Noted not fixed: prerender plus the interactive circuit each ran OnInitializedAsync, so every Service Health view was writing TWO audit entries; this change incidentally drops it to one, and whether other pages do the same is unexamined. Not done: the plan's manual acceptance checklist (no bUnit harness) and the implementation codereview. Also observed: the origin gitea remote is unreachable, SEC_E_CERT_EXPIRED. One session, opus-5; token usage not measured.

- 2026-09-18 click-gating slice 0A, the audit scanner as a repo tool (docs/ClickGatingAudit-Plan.md): the owner approved slice 0 + slice 1 + tier 1 only, so the plan status is now Approved in part with tiers 2-4 explicitly out of scope. This commit lands the instrument the audit was produced with, which until now existed only in a temp file and was therefore not durable memory. tools/Get-ClickGateAudit.ps1 plus 14 Pester tests. Three classification rules are the whole value and each was wrong in an earlier revision: (1) in-flight flags are detected STRUCTURALLY, never by name - this app uses actingDeviceId, isCsvProcessing, detailLoading, so a name list undercounts - and the test that a set-once latch is not a flag pins the revision that made every page look broken; (2) the flag must be lowered in a FINALLY, which is the only thing separating a gate from a nullable result field, and without it Migration read as 15 flags instead of its documented 8; (3) comments are BLANKED, not deleted, so reported line numbers still point at real code - deleting shifted every line, which is worse than reporting none because it sends the reader to innocent code. Calibrated against Migration as a known-good control: 8 named flags, IsBusy, the four documented exemptions at 223/442/784/836 and the three tab anchors at 30/33/36, all reproduced exactly. Full run reproduces the plan's published totals: 33 pages, 273 buttons, 108 ungated, 12 data-only, 89 partial, 209 gap, 7 non-button targets, 3 predicates. Disclosed limits kept honest in the header: text analysis not semantic analysis, no bUnit harness exists, findings are candidates for a human read, and the known false positives (expandedBatch, bulkOutcome, opResult, resolution, targetProtection, previewResult, detail) are named in the plan. Verified: 14/14 Pester, PSScriptAnalyzer clean on both new files with zero new findings repo-wide, ASCII lint passed. No C# touched, so no build or dotnet test needed for this commit. One session, opus-5 with ultracode; token usage not measured.

- 2026-09-18 click-gating reconnaissance, Revision 1 of docs/ClickGatingAudit-Plan.md: before building the shared harness, ran an 11-page read-only workflow over slice 1 + tier 1 - one deep read per page, two adversarial refutation lenses each (is the control-flow reading factually right / would the proposed gate trap the operator), then a synthesis designing the registry. 34 agents, 0 errors, ~4.2M subagent tokens, 69 minutes. It paid for itself several times over: it falsified four load-bearing claims in the design the owner had just approved, and every one would have shipped as a defect. (1) There is NO ErrorBoundary anywhere in the app (zero matches across Components/, Services/, Program.cs), so a throw from a handler tears the circuit down rather than leaving a live page with a stuck flag - which means the planned "lower every flag in a finally" assertion does not close the risk it was written for, and the real page-deadener is a call with no timeout or cancellation token (BlockedSenderService.UnblockSenderAsync:48 is the live example). (2) The plan prescribed a handler guard for non-button targets; on any DOM-synced control that CORRUPTS DATA, because the backing field is unchanged so the render diff emits no correction and the browser keeps the operator's action - on NamedLocations it writes the OLD country set to Graph permanently, since CountryCodePicker latches _initialized on first parameter push. Five refutations found this independently on five pages. Three refusal mechanisms are needed where the plan had one. (3) The scanner cannot see the controls that matter: its "7 non-button targets app-wide" is an @onclick-only count, while @onchange, InputFile, @onkeydown, @bind and child Disabled= are invisible - across these 11 pages alone the real number is about 35. (4) A page-wide predicate is provably wrong on SelfServiceGroups (two mutually exclusive views, the only "unsound" verdict) and ConferenceRooms (background OnJobChanged callback). Also caught: the obvious slice-1 fix is WRONG - wrapping BlockedSenders ConfirmUnblock 278-414 in try/finally moves the lowering after the LoadBlockedSenders call at 413, at which point a busy guard in that callee silently no-ops the post-unblock refresh and the table keeps showing the sender just unblocked. The remedy is a catch converting the throw into a PermissionResult.Fail, mirroring 351-356. Estimate for the approved scope revised 8-11 -> 20-29 sessions. Two live defects found in passing, neither caused nor fixed by this work and both needing an owner go: IntuneDevices.razor:403 WipeNameConfirmed is the ONLY enforcement of the typed device-name confirmation on a factory wipe (ExecuteActionAsync never re-reads wipeConfirmName), and BlockedSenderService.UnblockSenderAsync has no CancellationToken and no timeout. Lesson worth keeping: the recon was read-only and cost one workflow, and it caught a design flaw that all eleven planned assertions would have passed - the same lesson as the 2026-09-17 plan review, one level up. Records only this commit; ClickGateSource.cs is written and compiles but is held uncommitted because the registry it serves changed shape. Verification: git diff --check clean; no code touched. One session, opus-5 with ultracode.

- 2026-09-18 click-gating slice 0B, the shared tripwire suite and its registry (docs/ClickGatingAudit-Plan.md, owner approved option A): three new files replacing what would otherwise have been ~200 near-identical per-page tests. ClickGateSource.cs is the source reader generalised from MigrationStatusPageTests' per-page helpers, with two lessons baked in rather than left to each caller - comments are BLANKED not deleted so offsets and line numbers stay valid against the real file (EnclosingMethodName and every assertion message work from offsets, so a stripped-but-shifted source reports defects at innocent lines), and tag walking is quote-aware because a naive <button.*?> stops at the > inside a lambda arrow and reads a truncated tag. ClickGateRegistry.cs holds one entry per converted page plus a declared NotYetConverted list covering all 32 other pages with a reason each; every field in the record was forced by a specific page during the recon and the forcing page is named on the member so none gets dropped later as unused. ClickGateTests.cs has 14 assertions. Seeded with Migration ONLY - the single page whose gating is already known-good - so the suite is green today and each page joins as it is converted; registering the partial IntuneDevices/RiskyUsers predicates now would have shipped a red suite. The design win is EveryPageIsRegisteredOrDeclaredUnconverted: a new module's page is in neither list, so it fails and names itself, which makes the 2026-09-17 rule self-enforcing instead of dependent on whoever reviews the next module. Guard proof, four mutations, each failing exactly its named assertion at 1 failed / 15 passed with restores byte-identical by SHA256 and mtime touched: ungating a gated button, folding pendingActionLabel into IsBusy, deleting the tab-anchor handler guard, and removing a page from both registry lists. Honest limits written into the class doc rather than discovered later: no bUnit harness exists so nothing renders, text containment proves an identifier is present and never that it is load-bearing (disabled="@(IsBusy && false)" passes everything), the finally assertion does NOT close the risk the original plan wrote it for because there is no ErrorBoundary in the app and a throw kills the circuit regardless, and EveryNonButtonClickTargetIsRegistered only finds @onclick - @onchange, InputFile, @onkeydown, @bind and child Disabled= are invisible and must be hand-enumerated. Verified: build Release 0 errors and the same 23 pre-existing warnings, dotnet test 2526 passed / 0 failed / 3 skipped (up exactly 16), format --verify-no-changes exit 0, git diff --check exit 0, ASCII lint passed. No PowerShell touched this commit. Tests only, so no version bump. One session, opus-5 with ultracode.
- 2026-09-18 click-gating slice 1a, the BlockedSenders stuck flag (docs/ClickGatingAudit-Plan.md): ConfirmUnblock raised isLoading at the top and lowered it only by straight-line statements on three normal-exit paths, while the authorization round trip (GetAuthenticationStateAsync + AuthorizeAsync), the protected-principal gate and OperationTrace.BeginOperation all sat outside every try. The recon's warning was the whole value of this slice: the OBVIOUS fix is wrong. Wrapping the method in try/finally moves the isLoading clear after the LoadBlockedSenders call at the end, and once that callee consults a page-level busy predicate - which is the entire point of the sweep this slice unblocks - the confirming refresh silently no-ops and the operator is told the unblock succeeded while the sender stays listed. The fix is therefore a catch around the preflight that converts the throw into a PermissionResult.Fail and returns, mirroring the write catch already in the method, plus an audited UnblockSender_Denied row so a preflight that could not complete is still accountable. Fail closed: the catch returns rather than falling through, so an unreadable authorization answer can never become an unauthorized Exchange mutation. Six tripwires, all over comment-stripped source via ClickGateSource - which mattered here, because the new explanatory comments contain the literal strings "isLoading = false" and "LoadBlockedSenders" that two of the assertions search for. Guard proof, three mutations: hoisting the first preflight await above the try failed exactly PreflightAwaitsAreInsideTheTry + OnlyTaskYieldAwaitsBeforeTheTry (2 failed / 4 passed); deleting the isLoading clear from the preflight catch failed exactly PreflightCatchFailsClosed (1 failed / 5 passed); and the naive try/finally on the tail failed exactly LowersIsLoadingBeforeTheRefresh + HasNoFinally (2 failed / 4 passed). All restores byte-identical by SHA256 with the mtime touched. Worth recording: the first planned mutation - dropping the `return;` from the preflight catch - could not be run, because the compiler rejects it with CS0165 on the unassigned gate/scope locals. The fail-closed return is enforced by the type system, not only by the tripwire, which is a stronger guarantee than the test alone. Also hit and worth remembering: Serena's replace_content rewrites the whole file with CRLF on Windows, which fails `dotnet format` in this LF-normalised repo (.gitattributes forces eol=lf for .cs/.razor/.md); three files had to be converted back before the gate passed. Verified: build Release 0 errors and the same 23 pre-existing warnings, dotnet test 2532 passed / 0 failed / 3 skipped (up exactly 6), format --verify-no-changes exit 0, git diff --check exit 0, ASCII lint passed. No PowerShell touched. Module 1.4.0 -> 1.4.1, no base app bump. Not done: the page is NOT converted to a gated predicate - this slice only removes the prerequisite hazard - and the manual acceptance check needs a dev deploy because no bUnit harness exists. One session, opus-5 with ultracode.
- 2026-09-18 click-gating slice 1b, the MessageTrace stuck flag (docs/ClickGatingAudit-Plan.md): ToggleDetail raised detailLoading at 976 and lowered it at 987 and 996, both nested inside `if (token == detailRequestToken)`, with no finally anywhere in the method. The escape is the Audit.LogLookupAction at 992, which sits inside the catch and therefore outside every try - a throw there left the method with the flag raised and every detail button on the page dead for the life of the circuit. Fixed as a token-guarded finally. The token guard inside the finally is load-bearing and is NOT the obvious simplification: a bare clear would let a superseded fetch, completing after a newer click has taken the token, lower the NEWER request's flag and re-enable the button while that fetch is still in flight. Four tripwires; the negative one is per occurrence per the msr-1 lesson - it locates the finally with BlockAfter, then asserts every regex match of `detailLoading = false;` in the comment-stripped body falls inside that block, with no ordering assertion, so a method that both lowers correctly and leaks elsewhere still fails. Guard proof, four mutations, all real test failures rather than build failures: (A) the exact pre-fix shape, lowering back inside both token branches with the finally deleted, failed LowersDetailLoadingOnlyInsideTheFinally + FinallyKeepsTheTokenGuard at 2 failed / 8 passed; (B) the token guard dropped from the finally failed FinallyKeepsTheTokenGuard at 1/9; (C) deleting `detailRequestToken++` from RunTrace failed RunTrace_StillRescuesDetailLoadingAndSupersedesTheToken at 1/9; (D) moving the raise after the first await failed RaisesDetailLoadingBeforeItsFirstAwait at 1/9. All restores byte-identical by SHA256 with the mtime touched. Incidental confirmation that comment stripping works: the new explanatory comment inside the finally contains the literal `if (token == detailRequestToken)` and mutation B still bit. Verified: build Release 0 errors and the same 23 pre-existing warnings, dotnet test 2536 passed / 0 failed / 3 skipped (up exactly 4), format --verify-no-changes exit 0, git diff --check exit 0, ASCII lint passed. No PowerShell touched. Module 1.4.1 -> 1.4.2, no base app bump. **Slice 1 is now complete and tier 1 is unblocked.** Noticed and deliberately not fixed, as a separate finding for a separate commit: the unwrapped Audit.LogLookupAction calls inside catch blocks are the page idiom here (six sites across ToggleDetail, RunTrace, AnalyzeHeadersAsync, the CSV download and the email job). The new finally means a throw at 992 now lowers the flag on its way out, but the throw still escapes the handler and, with no ErrorBoundary in this app, takes the circuit. AnalyzeHeadersAsync has the same shape with its flag already safe. Implemented by a coding subagent under the owner's 2026-09-18 weekend ruling; orchestrator owned the records, the commit and the push.
- 2026-09-18 click-gating tier 1 page 1 of 9, DhcpAuthorization (docs/ClickGatingAudit-Plan.md, owner-approved option A): the first page through the shared harness, and the point of the exercise was to turn the per-page cost from an estimate into a measurement. Result: 362 -> 411 lines, ONE registry entry, ZERO new test methods - the 14 shared assertions instantiate for the new page and the full suite goes 2536 -> 2550, which is exactly +14. That is the economic claim of slice 0B holding up on first contact. Predicate is `isLoading || isOperating || isDownloadingCsv`; the third flag did not exist, because DownloadCsvAsync owned none, and its raise sits BELOW the `if (servers.Count == 0) return;` guard per Revision 1's warning that a raise above an early return leaks the flag true forever and, in a page-wide predicate, kills the whole page. Revision 1's census claim was re-verified rather than trusted (scanner plus a hand grep for @onchange/@bind/InputFile/@onkeydown/@onsubmit/Disabled=/anchors): zero non-button click targets confirmed. The three `@bind` inputs are DOM-synced so they take the disabled attribute - the only safe mechanism per falsification 2 - and are deliberately NOT registered as NonButtonTargets, because that list is keyed to what ClickGateSource can locate and an `<input>` is in neither Tags("a") nor NonButtonClickTargets(), so registering one would FAIL an assertion rather than document anything. Five snapshot-at-entry changes, each closing a named post-await live read: the two `operationResult.Success` reads after an email await (273 and 348) were the NRE the ungated dismiss could cause, which then audited and emailed a SUCCEEDED forest-level AD write as failed and skipped the confirming refresh. Guard proof, 8 mutations: M1 ungate a button, M2 the pre-fix CSV flag, M4 drop a preserved non-busy clause, M5 simplify a spinner into the predicate, M6 add a guard to the forbidden site, M7 drop an exemption's own narrower guard - each failed exactly its named assertion at 1 failed / 39 passed. M3a and M3b were caught by the COMPILER (CS0103), stronger than a test but reported as not-the-test-biting. **M3c is the real finding and is recorded as a gap, not smoothed over: a size-preserving revert of the operationResult snapshot passes every assertion in the suite** - only the incidental line-count fingerprint moved. That measures Revision 1's falsification 6, which said the design has no slot for the snapshot obligation; the only thing standing behind those two changes today is a code comment and registry prose. Two more unasserted fields recorded: PrerequisiteBeforeExemptionHolds (added to the record, no assertion - the agent could not touch ClickGateTests.cs under its scope) and RaiseMustFollowEarlyReturn (listed in Revision 1's design table, no field, no assertion). One obstacle hit and cleared correctly: renaming the empty-list guard broke a PRE-EXISTING tripwire pinning `if (servers.Count == 0)` shared by four sibling CSV pages, and the agent reordered its own code rather than touching the load-bearing guard. Verified: build Release 0 errors and the same 23 warnings, 2550 passed / 0 failed / 3 skipped, format exit 0, git diff --check exit 0, ASCII lint passed. Module 1.3.0 -> 1.4.0, no base app bump, matching the Migration precedent. Implemented by a coding subagent under the owner's 2026-09-18 weekend ruling. Eight pages remain.
- 2026-09-18 click-gating harness, the three missing assertions (docs/ClickGatingAudit-Plan.md): slice 0B shipped the registry without PostAwaitLiveReads, which Revision 1's design table listed as required and falsification 6 forced. Page 1's guard proof MEASURED the cost - mutation M3c, a size-preserving revert of the operationResult snapshot, passed all 40 assertions and was caught only incidentally by the line-count fingerprint - so the most important fix on that page stood on a code comment. This slice closes it before page 2, because falsification 6 names six pages. 14 -> 19 assertions, 40 -> 46 instantiated, full suite 2550 -> 2556, exactly 3 new theories x 2 registered pages. M3c now fails with the handler, the field and every offending line number. The field shape was derived from the page rather than invented, and needed a discriminator because there are genuinely two shapes: PublishedFromLocal, where the local is produced BY the await and then published (operationResult = result), which has no entry-time capture to check; and CapturedAtEntry, where the local IS the capture and must sit above the first await (newDnsName/newIpAddress/ticketNumber). The load-bearing subtlety is the definition of "read": WholeWord(id) NOT followed by an assignment, because `ticketNumber = "";` on the success path is a correct clear while `ticketNumber` passed to an audit call is the defect - without that distinction the assertion fails against correct code. PrerequisiteBeforeExemptionHolds is now asserted in BOTH directions and tied to machinery by parsing the field name out of the exemption's own registered snippet, so the note and the obligation cannot drift apart; direction A re-runs the real snapshot check rather than restating it. RaiseMustFollowEarlyReturn was implemented rather than documented-as-skipped, and its probe P4 was the ONLY failure out of 46, so nothing else in the suite sees that page-killing defect. Honest limits written into the comments rather than discovered later: all three are text containment, so none can prove the snapshot local is the value actually handed to the service call, and the early-return rule is textual ordering within one method body, not control flow. Guard proof, 8 probes, all size-preserving at 411 lines and compiling, all restored byte-identical with mtime touched: M3c 2F/44P, P2 (catch audits the live field) 1F/45P, P3 (capture hard-coded) 1F/45P, P4 1F/45P, P5a and P5b (registry edits in each direction) 1F/45P each, and P6a/P6b confirming ExtractBlock is not in this slice's dependency path - an unbalanced brace inside a string literal leaves the suite 46/46 clean. **Found and flagged before page 2: Migration cannot take a PostAwaitLiveReads entry as written.** It uses PublishedFromLocal correctly, but at 1495-1497 and 1727-1729 it does a deliberate null-safe save/restore across a refresh that itself nulls the field, which the read-after-first-await rule would flag as a false positive. Registering it needs a per-entry allow-list or a narrower dereference-only rule; the idiom may recur. Also noted: DhcpAuthorization line 83's ConditionThatKeepsItTrue is assertable but still prose, needing a Snapshot-less variant the page does not currently justify. Verified: build Release 0 errors / 23 warnings, 2556 passed / 0 failed / 3 skipped, format exit 0, git diff --check exit 0, ASCII lint passed, DhcpAuthorization.razor byte-identical to HEAD. Tests only, no version bump. No reviewer round dispatched: the 2026-08-31 decision makes those CRITICAL-only with an owner go, and the coder-side guard proof closes this.
