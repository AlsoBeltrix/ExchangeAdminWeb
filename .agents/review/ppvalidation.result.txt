{"verdict":"findings","capability_ok":true,"reviewed_sha":"521bb6e62741c7433a827079d1c53eef0b3b4fec","base_sha":"10d159363eeed955d825bf304a143594686b034b","findings":[{"title":"Cloud-only recipients addressed by secondary alias are returned as NotFound, bypassing NotFound-allowing gates","evidence":"Services/ProtectedPrincipalService.cs:352-360 treats any PrimarySmtpAddress differing from the typed identity as the alias case and immediately re-runs AD resolution; the cloud-only branch at Services/ProtectedPrincipalService.cs:379-389 is reached only when the primary address equals the typed identity. The ResolvedRecipient.ExistsOnPrem flag exists on Services/IIdentityResolver.cs:13-22 and is populated at Services/ExchangeIdentityResolver.cs:100-104, but ResolveWithExchangeFallbackAsync never uses it. ConferenceRoomProtectionGate allows when status is NotFound and resolved is null (Services/ConferenceRoomProtectionGate.cs:67-95), and GroupManagementService does the same (Services/GroupManagementService.cs:49-67).","predicted_failure":"If Exchange resolves vip.alias@contoso.com to primary vip.cloud@contoso.com with ExistsOnPrem=false, ResolveWithExchangeFallbackAsync re-queries AD for vip.cloud@contoso.com, gets NotFound, and returns no principal. ConferenceRooms or GroupManagement then treat the target as not protected, even if the protected user list contains vip.cloud@contoso.com; the same cloud-only principal is denied when addressed by its primary address.","severity":"HIGH","better_approach":"Branch on ExistsOnPrem, not address equality alone: when Exchange says the recipient is not on-prem-backed, construct the null-DN ResolvedDirectoryPrincipal using the canonical primary address regardless of whether the operator typed a secondary alias. Only re-resolve the canonical address in AD for on-prem-backed recipients."},{"title":"DOMAIN-prefix normalization corrupts escaped distinguished names before validation","evidence":"ADDirectorySearchService.BuildExactMatchFilter normalizes every identity before building user, group, or OU filters (Services/ADDirectorySearchService.cs:405-413). NormalizeIdentity strips everything through the first backslash unless it is trailing (Services/ADDirectorySearchService.cs:423-433). The admin page stores group and OU selections as DNs via ReturnValueKind=\"DN\" (Components/Pages/AdminSettings.razor:150 and Components/Pages/AdminSettings.razor:172), and CanonicalValue also stores group/OU DNs (Services/ProtectedPrincipalEntryValidator.cs:128-135).","predicted_failure":"A valid AD DN containing an escaped comma, for example OU=Sales\\, East,DC=contoso,DC=com or CN=VIP\\, Tier0,OU=Groups,DC=contoso,DC=com, is normalized to the substring after the escape backslash before the LDAP filter is built. Adding it from the picker or pasting it is refused as 'not found in Active Directory', and an already-saved valid entry can be badged stale, encouraging removal of a working protection rule.","severity":"MEDIUM","better_approach":"Make DOMAIN\\ stripping object-kind and shape aware. Do not strip backslashes from DNs; only strip a domain prefix for simple user/group names that match a DOMAIN\\name form, then pass DNs through to EscapeLdapFilter unchanged."},{"title":"Save can persist a stale protected-principal list while validation is still in flight","evidence":"The Save Protected Principals button is disabled only by saving, not ppValidating (Components/Pages/AdminSettings.razor:99). AddValidatedAsync sets ppValidating and awaits the synchronous AD lookup on a background task (Components/Pages/AdminSettings.razor:534-543), so the circuit can process another click while the add is pending. SaveProtectedPrincipals snapshots ppUsers/ppGroups/ppOus and saves them without checking ppValidating (Components/Pages/AdminSettings.razor:581-590).","predicted_failure":"An admin clicks Add for a valid protected principal, sees 'Checking...', then clicks Save before validation returns. SaveConfig persists the old list without the pending entry; afterward the validation completes and the entry appears in the page with the input cleared, while the status still says the protected principals were saved. A reload loses the entry and the intended protection is not active.","severity":"MEDIUM","better_approach":"Disable Save while ppValidating is true, or have SaveProtectedPrincipals refuse with a retry message while validation is pending. For stronger behavior, await or cancel the pending validation before taking the list snapshot."},{"title":"New live AD tests still pass silently when the live dependency is absent","evidence":"ADDirectoryLiveTests documents that it no-ops when AD is not reachable (ExchangeAdminWeb.Tests/ADDirectoryLiveTests.cs:6-18), and each live test returns early on !Reachable instead of marking the test skipped (ExchangeAdminWeb.Tests/ADDirectoryLiveTests.cs:53-55, 73-75, 85-87, 104-106, 115-117).","predicted_failure":"On CI or any review host without a reachable ActiveDirectory module, these tests report green even if the OU mapping or live ValidateExists path is deliberately broken. That makes the live coverage indistinguishable from a pass rather than an explicit skipped dependency.","severity":"LOW","better_approach":"Replace the early returns with Assert.SkipWhen(!Reachable(svc), \"Active Directory is not reachable\") or an equivalent explicit skip so the result is visible as skipped, not passed."}]}