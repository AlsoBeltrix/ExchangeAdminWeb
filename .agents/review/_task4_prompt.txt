You are an independent code reviewer. You are reviewing ONE landed slice on this git
repository. You are the reviewer role in a two-role review loop; your job is to judge
the fix against its recorded finding, not to rubber-stamp it. A pass that finds no
material issue is a complete, valid, EXPECTED result -- do not manufacture findings.

REPO: D:\source\ExchangeAdminWeb  (ASP.NET Core 10 Blazor Server, net10.0-windows)

SCOPE (fixed snapshot):
  base SHA: aba97df
  head SHA: f17f3de
  Review: git diff aba97df..f17f3de

Read the finding record at .agents/review/findings/gm3-task4-slice1.md for the full
mandate and the AC9 invariant. Read the code from the workspace at the head SHA; do not
trust my description.

This slice is GM-3 task 4: an in-list filter (AC9) over the already-loaded owned-groups
list. It adds a pure helper (Services/SelfServiceGroups/ManageableGroupFilter.cs), wires
it into Components/Pages/SelfServiceGroups.razor, and adds
ExchangeAdminWeb.Tests/ManageableGroupFilterTests.cs (12 tests). No new directory access
or service call.

Check especially:
  - AC9: the match is a NON-PREFIX substring (a mid-name word and a description word both
    find the group), case-insensitive, across name/sAMAccountName/description; a blank
    term returns the whole list; order is preserved.
  - No directory round-trip: the filter reads only the in-memory loaded list.
  - A filtered-to-empty result is a distinct filter message, NOT the AC8 load-failure
    error and NOT a silent drop.
  - Filtering cannot widen what was loaded (it only hides rows) -- no new authz surface.
  - filterTerm is reset on each load so a stale filter cannot hide new results.

INDEPENDENT GUARD PROOF (do this yourself, in an isolated snapshot -- never mutate the
main working tree). HOST NOTE (this Windows machine, sandbox=workspace-write): `git
worktree add` and `git clone` are BLOCKED here (the sandbox denies writes under the
repo's .git/ and the clone helper's shared-memory mapping fails). Snapshot the head SHA
into a temp dir with:
  $r = Join-Path ([IO.Path]::GetTempPath()) ("ssg-review-" + [Guid]::NewGuid().ToString('N'))
  New-Item -ItemType Directory -Path $r | Out-Null
  $z = Join-Path $r 'snap.zip'
  git archive --format=zip --output=$z f17f3de
  Expand-Archive -LiteralPath $z -DestinationPath $r -Force
then in $r run:
  dotnet test ExchangeAdminWeb.slnx --filter "FullyQualifiedName~ManageableGroupFilterTests"
(targeting the .slnx is required; bare dotnet test runs zero tests). Prove non-vacuity:
change ManageableGroupFilter.Contains from `haystack.Contains(needle, ...)` to
`haystack.StartsWith(needle, ...)` in the snapshot, re-run (expect FAIL -- the non-prefix
tests break), restore, re-run (expect PASS 12/12). Set guard_confirmed true only if you
observed revert->FAIL and restore->PASS yourself. Do NOT try worktree/clone first.

Return ONLY this JSON object as your final output (no prose around it):
{"verdict":"accepted|reopened|invalid","guard_confirmed":true,
 "reviewed_sha":"f17f3de","base_sha":"aba97df","comments":["file:line -- ..."]}
